# 迁移映射表（com.jushan.system → com.jushan.platform.modules）

> 生成于阶段 0 任务包 0-1。旧包 `com.jushan.system` 已冻结（各包 `package-info.java` 标记 @Deprecated），只删不增。
>
> Spring 扫描现状：`ParkingApplication` 使用 `@SpringBootApplication(scanBasePackages = "com.jushan")`，新旧两包均被扫描；`MyBatisConfig` 通过两个 MapperScannerConfigurer 分别扫描 `com.jushan.system.mapper`（无前缀）与 `com.jushan.platform.modules.**.mapper`（bean 名加 `modules` 前缀）。

迁移批次总览：
- 批次1（任务包 0-2）：计费规则收敛（BillingRule → FeeRule）
- 批次2（任务包 0-3）：订单/支付/进出场迁移 + Pyun 隔离到 parking.pay.pyun
- 批次3：设备/车场/识别/WebSocket 域
- 批次4：组织/租户/账号/权限/审计域（含 mybatis 租户支撑类迁往 infrastructure）
- 批次5：车辆/小程序/归档/监控等其余类

| 旧包类 | 新包对应类（已有/待建） | 依赖它的类（旧包以外） | 建议迁移批次 |
| --- | --- | --- | --- |
| `com.jushan.system.dto.AuditLogQueryRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.BindPhoneRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.BindPlateRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.ChangePasswordRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.CreateBillingRuleRequest` | com.jushan.platform.modules.parking.dto.FeeRuleCreateCmd（已有） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.dto.CreateCompanyRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.CreateDeviceRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.CreateEmployeeRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.CreateLaneRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.CreateParkingLotRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.FeePreviewRequest` | com.jushan.platform.modules.parking.dto.FeeCalculateCmd（已有） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.dto.FixedSpaceCreateRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.FixedSpaceRenewRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.MockRecognitionRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.MonthlyPassCreateRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.MonthlyPassRenewRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.ParkingLotCapacityRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.ParkingLotStatusRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.ProxyRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.RegisterRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test:tenant/TenantServiceAuditApprovedTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.RemoteGateAlertDTO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.ResetPasswordRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.SwitchBillingRuleRequest` | com.jushan.platform.modules.parking.dto.FeeRuleSwitchCmd（待建） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.dto.SystemParamUpdateRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.TenantAuditRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test:tenant/TenantServiceAuditApprovedTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.UnbindPlateRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.dto.UpdateBillingRuleRequest` | com.jushan.platform.modules.parking.dto.FeeRuleUpdateCmd（已有） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.dto.UpdateCompanyRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.UpdateDeviceRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.UpdateEmployeeRequest` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.dto.UpdateLaneRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.UpdateParkingLotRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.dto.WxLoginRequest` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.AuditLogVO` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.vo.BillingRuleVO` | com.jushan.platform.modules.parking.vo.FeeRuleVO（已有） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.vo.BillingRuleVersionVO` | com.jushan.platform.modules.parking.vo.FeeRuleSegmentVO（已有） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.vo.BoothLaneVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.BoothMonitorSnapshotVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.BoothRecognitionEventVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.CompanyTreeVO` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.vo.CompanyVO` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.vo.DashboardVO` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.vo.DeviceModelVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.DeviceStatusVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.DeviceVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.DeviceVendorVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.EmployeeVO` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.vo.ExceptionAdminVO` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.FixedSpaceVO` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.ManualGateRecordAdminVO` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.MonitorAlertVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.MonthlyPassVO` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.OrderAdminVO` | com.jushan.platform.modules.parking.vo.OrderAdminVO（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.vo.ParkingFeeVO` | com.jushan.platform.modules.parking.vo.ParkingFeeVO（待建） | modules:modules/miniapp/controller/MiniPayController | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.vo.ParkingLaneVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.ParkingLotReadinessVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.ParkingLotVO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.ParkingRecordAdminVO` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.PlateBindingVo` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.ReadinessItem` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.vo.SystemParamVO` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.vo.TenantVO` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.vo.WxLoginResult` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.vo.WxUserVo` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mybatis.TenantIgnore` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mybatis.TenantIgnoreAspect` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mybatis.TenantMetaObjectHandler` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.ArchiveData` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.ArchiveJobLog` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.BaseEntity` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.BillingRule` | com.jushan.platform.modules.parking.entity.FeeRule（已有） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>boot-test:service/BillingEngineTest | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.entity.BillingRuleRecalcLog` | com.jushan.platform.modules.parking.entity.FeeRuleRecalcLog（待建） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.entity.BillingRuleSwitchLog` | com.jushan.platform.modules.parking.entity.FeeRuleSwitchLog（待建） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.entity.BillingRuleVersion` | com.jushan.platform.modules.parking.entity.FeeRule + FeeRuleSegment（已有，版本语义并入） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>boot-test:service/BillingEngineTest | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.entity.BindingPolicy` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.Company` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:TenantContextIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.Device` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:MockRecognitionIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules:modules/device/webhook/DeviceWebhookService<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.DeviceCommandAudit` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:OpenGatePlaceholderIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.DeviceModel` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:OpenGatePlaceholderIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.DeviceStatusSnapshot` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DeviceStatusIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.DeviceVendor` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.DuplicateEntryLog` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.EmployeeParkingLot` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:TenantContextIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.ExceptionRecord` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.ExitRecord` | com.jushan.platform.modules.parking.entity.ExitRecord（待建） | boot-test-archived:ExitFlowIntegrationTest<br>boot-test:service/ExitServiceTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.entity.FixedSpaceBinding` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.LegacySysRolePermission` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.MockPaymentConfig` | com.jushan.platform.modules.parking.pay.mock.entity.MockPaymentConfig（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.entity.MockPaymentRecord` | com.jushan.platform.modules.parking.pay.mock.entity.MockPaymentRecord（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.entity.MonitorAlert` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.ParkingLane` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:MockRecognitionIntegrationTest<br>boot-test-archived:ParkingLaneControllerIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules:modules/device/webhook/DeviceWebhookService | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.ParkingLot` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:MockRecognitionIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>boot-test-archived:TenantContextIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>modules:modules/miniapp/controller/MiniParkingLotController<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/miniapp/service/impl/MiniUserServiceImpl | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.ParkingLotCapacityLog` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:ParkingLotControllerIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.ParkingLotStatusLog` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:ParkingLotControllerIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.ParkingOrder` | com.jushan.platform.modules.parking.entity.ParkingOrder（待建，表名不变） | boot-test-archived:ExitFlowIntegrationTest<br>boot-test:service/ExitServiceTest<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/miniapp/service/impl/MiniUserServiceImpl<br>modules:modules/parking/service/impl/ParkingSessionServiceImpl<br>modules:modules/vehicle/controller/SysVehicleController<br>modules:modules/vehicle/listener/RenewalPaymentListener<br>modules:modules/vehicle/service/impl/VehicleRenewalServiceImpl<br>modules-test:modules/vehicle/listener/RenewalPaymentListenerTest<br>modules-test:modules/vehicle/service/impl/VehicleRenewalServiceImplTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.entity.ParkingRecord` | com.jushan.platform.modules.parking.entity.ParkingRecord（待建，表名不变；与 ParkingSession 关系批次2内裁定） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>boot-test:service/ExitServiceTest<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/miniapp/service/impl/MiniUserServiceImpl<br>modules:modules/parking/service/impl/ParkingSessionServiceImpl | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.entity.ParkingRecordSyncLog` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.PayMerchantConfig` | com.jushan.platform.modules.parking.pay.pyun.entity.PayMerchantConfig（隔离迁移） | 仅旧包内部使用 | 批次2(任务包0-3 Pyun隔离) |
| `com.jushan.system.entity.PayOrder` | com.jushan.platform.modules.parking.pay.entity.PayOrder（待建，表名不变） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.entity.PlateBinding` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | modules:modules/miniapp/service/impl/MiniUserServiceImpl | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.RecognitionEventLog` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:MockRecognitionIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.entity.SysAuditLog` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:ProxyAuditIntegrationTest<br>modules:task/OverstayBlacklistService<br>modules-test:task/OverstayBlacklistServiceTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.SysConfig` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.SysLoginLog` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.SysPermission` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.SysRole` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.SysUser` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:AuthControllerIntegrationTest<br>boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLaneControllerIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:ProxyAuditIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest<br>boot-test-archived:RolePermissionIntegrationTest<br>boot-test-archived:TenantContextCustomTokenTest<br>boot-test-archived:TenantContextIntegrationTest<br>boot-test-archived:TenantRegistrationIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.Tenant` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLaneControllerIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:ProxyAuditIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest<br>boot-test-archived:TenantContextIntegrationTest<br>boot-test-archived:TenantRegistrationIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.TenantAuditLog` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.entity.Vehicle` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | modules:modules/miniapp/service/impl/MiniUserServiceImpl | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.VehicleRenewalLog` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.entity.WxUser` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | modules:modules/miniapp/controller/MiniPayController | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.ArchiveDataMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.ArchiveJobLogMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.BillingRuleMapper` | com.jushan.platform.modules.parking.mapper.FeeRuleMapper（已有） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>boot-test:service/BillingEngineTest | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.mapper.BillingRuleRecalcLogMapper` | com.jushan.platform.modules.parking.mapper.FeeRuleRecalcLogMapper（待建） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.mapper.BillingRuleSwitchLogMapper` | com.jushan.platform.modules.parking.mapper.FeeRuleSwitchLogMapper（待建） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.mapper.BillingRuleVersionMapper` | com.jushan.platform.modules.parking.mapper.FeeRuleSegmentMapper（已有） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>boot-test:service/BillingEngineTest | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.mapper.BindingPolicyMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.CompanyMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:TenantContextIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.DashboardMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.DeviceCommandAuditMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:OpenGatePlaceholderIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.DeviceMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules:modules/device/webhook/DeviceWebhookService<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.DeviceModelMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:OpenGatePlaceholderIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.DeviceStatusSnapshotMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DeviceStatusIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.DeviceVendorMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.DuplicateEntryLogMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.EmployeeParkingLotMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:TenantContextIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.ExceptionRecordMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.ExitRecordMapper` | com.jushan.platform.modules.parking.mapper.ExitRecordMapper（待建） | boot-test-archived:ExitFlowIntegrationTest<br>boot-test:service/ExitServiceTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.mapper.FixedSpaceBindingMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.LegacySysRolePermissionMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.MockPaymentConfigMapper` | com.jushan.platform.modules.parking.pay.mock.mapper.MockPaymentConfigMapper（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.mapper.MockPaymentRecordMapper` | com.jushan.platform.modules.parking.pay.mock.mapper.MockPaymentRecordMapper（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.mapper.MonitorAlertMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.ParkingLaneMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:ParkingLaneControllerIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules:modules/device/webhook/DeviceWebhookService<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.ParkingLotCapacityLogMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:ParkingLotControllerIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.ParkingLotMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLaneControllerIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>boot-test-archived:TenantContextIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>boot-test:service/ExitServiceTest<br>modules:modules/miniapp/controller/MiniParkingLotController<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/miniapp/service/impl/MiniUserServiceImpl | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.ParkingLotStatusLogMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:ParkingLotControllerIntegrationTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.ParkingOrderMapper` | com.jushan.platform.modules.parking.mapper.ParkingOrderMapper（待建） | boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/miniapp/service/impl/MiniUserServiceImpl<br>modules:modules/parking/service/impl/ParkingSessionServiceImpl<br>modules:modules/vehicle/service/impl/VehicleRenewalServiceImpl<br>modules-test:modules/vehicle/service/impl/VehicleRenewalServiceImplTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.mapper.ParkingRecordMapper` | com.jushan.platform.modules.parking.mapper.ParkingRecordMapper（待建） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:WxParkingFeeControllerIntegrationTest<br>boot-test:service/ExitServiceTest<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/miniapp/service/impl/MiniUserServiceImpl<br>modules:modules/parking/service/impl/ParkingSessionServiceImpl | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.mapper.ParkingRecordSyncLogMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.PayMerchantConfigMapper` | com.jushan.platform.modules.parking.pay.pyun.mapper.PayMerchantConfigMapper（隔离迁移） | 仅旧包内部使用 | 批次2(任务包0-3 Pyun隔离) |
| `com.jushan.system.mapper.PayOrderMapper` | com.jushan.platform.modules.parking.pay.mapper.PayOrderMapper（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.mapper.PlateBindingMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | modules:modules/miniapp/service/impl/MiniUserServiceImpl | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.RecognitionEventLogMapper` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:MockRecognitionIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.mapper.SysAuditLogMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:ProxyAuditIntegrationTest<br>modules:task/OverstayBlacklistService<br>modules-test:task/OverstayBlacklistServiceTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.SysConfigMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.SysLoginLogMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:AuthControllerIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.SysPermissionMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.SysRoleMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | modules:modules/auth/controller/AuthController | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.SysUserMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:AuthControllerIntegrationTest<br>boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLaneControllerIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:ProxyAuditIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest<br>boot-test-archived:RolePermissionIntegrationTest<br>boot-test-archived:TenantContextCustomTokenTest<br>boot-test-archived:TenantContextIntegrationTest<br>boot-test-archived:TenantRegistrationIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.TenantAuditLogMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.TenantMapper` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test-archived:BoothParkingFeeControllerIntegrationTest<br>boot-test-archived:DeviceControllerIntegrationTest<br>boot-test-archived:DeviceStatusIntegrationTest<br>boot-test-archived:DeviceTimeSyncIntegrationTest<br>boot-test-archived:EmployeeIntegrationTest<br>boot-test-archived:LaneDeviceBindingIntegrationTest<br>boot-test-archived:OpenGatePlaceholderIntegrationTest<br>boot-test-archived:ParkingLaneControllerIntegrationTest<br>boot-test-archived:ParkingLotControllerIntegrationTest<br>boot-test-archived:ProxyAuditIntegrationTest<br>boot-test-archived:ReadinessCheckIntegrationTest<br>boot-test-archived:TenantContextIntegrationTest<br>boot-test-archived:TenantRegistrationIntegrationTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.mapper.VehicleMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | modules:modules/miniapp/service/impl/MiniUserServiceImpl | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.VehicleRenewalLogMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.mapper.WxUserMapper` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | modules:modules/miniapp/controller/MiniPayController | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.controller.ArchiveController` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.controller.AuditController` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.controller.BillingRuleController` | com.jushan.platform.modules.parking.controller.FeeRuleController（已有，需对齐接口） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.controller.BoothAuthController` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.controller.BoothMonitorController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.BoothParkingFeeController` | com.jushan.platform.modules.booth.controller.BoothParkingFeeController（待建，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.controller.BoothParkingLotController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.CompanyController` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.controller.DashboardController` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.controller.DeviceController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.EmployeeController` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.controller.ExceptionRecordAdminController` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.controller.FixedSpaceController` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.controller.InternalGateController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.ManualGateRecordAdminController` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.controller.MockPaymentController` | com.jushan.platform.modules.parking.pay.mock.MockPaymentController（待建，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.controller.MockRecognitionController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.MonthlyPassController` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.controller.OrderAdminController` | com.jushan.platform.modules.parking.controller.OrderAdminController（待建，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.controller.ParkingLaneController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.ParkingLotController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.ParkingOrderController` | com.jushan.platform.modules.parking.controller.ParkingOrderController（待建，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.controller.ParkingRecordAdminController` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.controller.PyunNotifyController` | com.jushan.platform.modules.parking.pay.pyun.PyunNotifyController（隔离迁移，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 Pyun隔离) |
| `com.jushan.system.controller.PyunPpFrontController` | com.jushan.platform.modules.parking.pay.pyun.PyunPpFrontController（隔离迁移，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 Pyun隔离) |
| `com.jushan.system.controller.PyunVipController` | com.jushan.platform.modules.parking.pay.pyun.PyunVipController（隔离迁移，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 Pyun隔离) |
| `com.jushan.system.controller.RemoteGateController` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.controller.SystemParamController` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.controller.TenantController` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.controller.WxParkingFeeController` | com.jushan.platform.modules.miniapp.controller.WxParkingFeeController（待建，URL不变） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.controller.WxUserController` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.service.ArchiveService` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.service.AuditService` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | modules:modules/vehicle/service/impl/VehicleRenewalServiceImpl<br>modules-test:modules/vehicle/service/impl/VehicleRenewalServiceImplTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.service.BillingEngine` | com.jushan.platform.modules.parking.service.FeeCalculationService（已有接口，需以旧引擎语义实现） | boot-test:service/BillingEngineTest<br>boot-test:service/ExitServiceTest<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules:modules/miniapp/controller/MiniPayController<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.service.BillingRuleConfig` | 并入 FeeRule 实体字段（待建） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.service.BillingRuleService` | com.jushan.platform.modules.parking.service.FeeRuleService（已有，需补齐实现） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.service.BoothMonitorService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.CompanyService` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.service.DeviceService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:OpenGatePlaceholderIntegrationTest<br>modules:modules/booth/controller/RecognitionEventController<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.DuplicateEntryHandler` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.DuplicateEntryPolicy` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.EmployeeService` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.service.EntryService` | com.jushan.platform.modules.parking.service.EntryService（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.ExitResult` | com.jushan.platform.modules.parking.service.ExitResult（待建） | boot-test-archived:ExitFlowIntegrationTest<br>boot-test:service/ExitServiceTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.ExitService` | com.jushan.platform.modules.parking.service.ExitService（待建） | boot-test-archived:ExitFlowIntegrationTest<br>boot-test:service/ExitServiceTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.FixedSpaceService` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | boot-test:service/ExitServiceTest<br>modules:modules/vehicle/service/impl/VehicleTypeDecisionServiceImpl<br>modules-test:modules/vehicle/service/impl/VehicleTypeDecisionServiceImplTest | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.service.GpioGateService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.MockPaymentService` | com.jushan.platform.modules.parking.pay.mock.MockPaymentService（待建，唯一支付入口语义保留） | modules:modules/miniapp/controller/MiniPayController | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.MockRecognitionService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.MonitorAlertService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules:task/OverstayBlacklistService<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest<br>modules-test:task/OverstayBlacklistServiceTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.MonthlyPassService` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | 仅旧包内部使用 | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.service.ParkingFeeService` | com.jushan.platform.modules.parking.service.ParkingFeeService（待建） | modules:modules/miniapp/controller/MiniPayController | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.ParkingLaneService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.ParkingLotScopeResolver` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.ParkingLotService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.ParkingOrderService` | com.jushan.platform.modules.parking.service.ParkingOrderService（待建） | boot-test:service/ExitServiceTest<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/vehicle/controller/SysVehicleController | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.PermissionService` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.service.PrepaidDeductionService` | com.jushan.platform.modules.parking.pay.mock.PrepaidDeductionService（待建） | boot-test:service/ExitServiceTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.PyunParkingSyncService` | com.jushan.platform.modules.parking.pay.pyun.PyunParkingSyncService（隔离迁移） | 仅旧包内部使用 | 批次2(任务包0-3 Pyun隔离) |
| `com.jushan.system.service.PyunPaymentClient` | com.jushan.platform.modules.parking.pay.pyun.PyunPaymentClient（隔离迁移） | 仅旧包内部使用 | 批次2(任务包0-3 Pyun隔离) |
| `com.jushan.system.service.ReadinessCheckService` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.service.ReleaseDecision` | com.jushan.platform.modules.parking.service.ReleaseDecision（待建） | 仅旧包内部使用 | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.service.SystemParamService` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | 仅旧包内部使用 | 批次4(组织/账号/权限域) |
| `com.jushan.system.service.TenantService` | com.jushan.platform.modules.company / tenant / account / auth / log（账号角色权限已有 Sys* 对应） | boot-test:tenant/TenantServiceAuditApprovedTest | 批次4(组织/账号/权限域) |
| `com.jushan.system.service.TimeSegmentConfig` | 并入 FeeRuleSegment 实体（已有） | 仅旧包内部使用 | 批次1(任务包0-2 计费收敛) |
| `com.jushan.system.service.WxUserService` | com.jushan.platform.modules.vehicle / miniapp / parking（WxUser、PlateBinding、FixedSpace、MonthlyPass、Archive 等待建） | modules:modules/miniapp/controller/MiniMessageController<br>modules:modules/miniapp/controller/MiniPayController<br>modules:modules/miniapp/service/impl/MiniUserServiceImpl | 批次5(车辆/小程序/归档/监控域) |
| `com.jushan.system.event.EventSource` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>boot-test:service/ExitServiceTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.event.PaymentSuccessEvent` | com.jushan.platform.modules.parking.pay.event.PaymentSuccessEvent（待建） | modules:modules/vehicle/listener/RenewalPaymentListener<br>modules-test:modules/vehicle/listener/RenewalPaymentListenerTest | 批次2(任务包0-3 订单/支付迁移) |
| `com.jushan.system.event.PlateStandardizer` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.event.RecognitionEventConsumer` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.event.RecognitionEventPayload` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test-archived:DuplicateEntryPolicyTest<br>boot-test-archived:EntryServiceTest<br>boot-test-archived:ExitFlowIntegrationTest<br>boot-test-archived:RecognitionEventConsumerTest<br>boot-test:service/ExitServiceTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.event.RecognitionEventPublisher` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.DeviceAccessClient` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test:client/DeviceAccessClientControlTest<br>boot-test:client/DeviceAccessClientRobustnessTest<br>boot-test:client/DeviceAccessClientTest<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.DeviceAccessClientImpl` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test:client/DeviceAccessClientControlTest<br>boot-test:client/DeviceAccessClientRobustnessTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.CommandResultDTO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | modules:modules/booth/controller/RecognitionEventController<br>modules:modules/booth/service/impl/RecognitionEventServiceImpl<br>modules-test:modules/booth/service/impl/RecognitionEventServiceImplTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.DeviceAccessResponse` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.DeviceStatusDTO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test:client/DeviceAccessClientRobustnessTest<br>boot-test:client/DeviceAccessClientTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.DisplayConfigRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.DisplayResultDTO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.DisplaySaveRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.DisplayTextRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.TimeSyncResultDTO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test:client/DeviceAccessClientRobustnessTest<br>boot-test:client/DeviceAccessClientTest | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.VoiceControlRequest` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.client.dto.VoiceResultDTO` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.ws.BoothTopicAccessChecker` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | 仅旧包内部使用 | 批次3(设备/车场/识别域) |
| `com.jushan.system.ws.BoothWebSocketPublisher` | com.jushan.platform.modules.device / parking / booth（部分已有：ParkingLot/ParkingLane 双份待收敛，device 域实体待建） | boot-test:service/ExitServiceTest<br>modules:modules/miniapp/controller/MiniPayController | 批次3(设备/车场/识别域) |
