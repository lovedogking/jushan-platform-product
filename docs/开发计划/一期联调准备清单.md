# 一期联调准备清单

> 文档状态：**VERIFIED 2026-07-15 - 核心闭环通过**
> 适用范围：jushan-platform 一期 MVP 联调准备（与 Device Access v0.4 + P云支付）
> 最后更新：2026-07-15
> 关联文档：[联调前阻塞项检查清单](./联调前阻塞项检查清单.md)、[PRD V1.1](../需求文档/停车SaaS系统完整需求文档_PRD_V1.0_最终定稿.md)、[AGENTS.md](../../AGENTS.md)

---

## 1. 联调目标

| 目标 | 说明 | 状态 |
|------|------|------|
| 识别→计费→收费→放行 完整闭环 | 一条标准车场完成临时车/月卡车全流程 | ✅ 已验证（临时车闭环通过） |
| Device Access v0.4 HTTP 接口联调 | 设备管理、开闸、关闸、显示屏、语音 | ✅ 核心接口通过，门控接口待真机 |
| Webhook 车牌识别事件推送联调 | Device Access → 平台创建/匹配停车记录 | ✅ 平台侧处理逻辑已验证 |
| P云支付预下单/支付通知联调 | 被扫模式支付 + 回调通知 | ⏳ 模拟模式通过，真回调待联调 |

---

## 2. 环境准备清单

| 检查项 | 验收标准 | 负责人 | 状态 | 备注 |
|--------|---------|--------|------|------|
| Device Access v0.4 服务启动 | HTTP 可达，/actuator/health 返回 UP | | ✅ | localhost:8081，E2E 全程可达 |
| 平台后端服务启动 | parking-boot 无 ERROR 日志，/actuator/health 返回 UP | | ✅ | localhost:8080，UP (DB/Redis/RabbitMQ 全部就绪) |
| MySQL 数据库初始化 | Flyway 迁移全部执行成功，无重复执行错误 | | ✅ | 57 个迁移全部 success=1 |
| Redis 服务启动 | ping 返回 PONG | | ✅ | 7.4.9，E2E 验证通过 |
| RabbitMQ 服务启动 | 管理界面可访问，默认 guest/guest | | ✅ | 3.12.14，健康检查通过 |
| 运营后台前端 (admin-web) | `pnpm dev` 成功，登录页可访问 | | ✅ | :3000 可访问 |
| 岗亭端前端 (booth-web) | `pnpm dev` 成功，监控页可访问 | | ✅ | :3001 可访问 |
| 小程序开发者工具 | 项目可编译，基础库版本 ≥3.5.0 | | ⏳ | 已修复金额精度，真机联调时验证 |
| VPN/内网可达 | 平台 ↔ Device Access 网络互通 | | ✅ | 同机部署，localhost 互通 |
| 端口白名单 | 平台 8080(后管)/8081(岗亭) 端口放行 | | ✅ | 开发/联调环境正常 |

### 判断标准

- **通过**：所有环境启动成功，相互网络可达
- **失败**：任一环境无法启动或网络不通，排查后重试

---

## 3. 数据准备清单

| 检查项 | 验收标准 | 负责人 | 状态 | 备注 |
|--------|---------|--------|------|------|
| 租户创建 | 通过注册/超管创建至少 1 个租户 | | ✅ | 租户 ID=1，E2E 全程使用 |
| 停车场创建 | 在租户下创建 1 个标准停车场（入口+出口） | | ✅ | 测试停车场 lotId=1 |
| 入口车道配置 | 配置 1 条入口车道，方向=ENTRY | | ✅ | 车道 ID=1，type=1(入口) |
| 出口车道配置 | 配置 1 条出口车道，方向=EXIT | | ✅ | 车道 ID=2，type=2(出口) |
| 设备台账 | 添加相机 + 道闸设备，记录 deviceSn | | ✅ | DA 注册 + 平台台账同步，deviceSn=TEST_CAM_001 |
| 设备绑定 | 将设备绑定到对应车道 | | ✅ | laneId=1/2 |
| 收费规则配置 | 配置至少 1 条按时长阶梯费率 | | ✅ | 首小时 5 元，后续 2 元/小时 |
| 月卡配置 | 配置 1 条月卡规则 | | ⏳ | E2E 未覆盖，后续补充 |
| 测试车辆登记 | 录入至少 2 台测试车辆（临时车+月卡车） | | ✅ | 京 PAY001（临时车）已验证 |

### 判断标准

- **通过**：数据全部准备完毕，可在 admin-web 页面和 API 级验证
- **部分通过**：基础数据就绪，月卡/特殊规则可推迟到第二轮联调

---

## 4. Device Access 联调项

### 4.1 设备管理接口

| 接口 | 验证内容 | 请求示例 | 预期结果 | 状态 |
|------|----------|---------|----------|------|
| `POST /api/v1/devices` | 设备注册 | `{"deviceSn":"TEST_CAM_001","vendor":"ZHENSHI","model":"C5H","type":"CAMERA"}` | 201 Created，返回 deviceId | ✅ |
| `GET /api/v1/devices` | 设备列表查询 | — | 200 OK，返回设备列表 | ✅ |
| `GET /api/v1/devices/{deviceId}` | 设备详情查询 | — | 200 OK，返回设备详情 | ✅ |
| `PUT /api/v1/devices/{deviceId}` | 设备更新 | `{"name":"入口相机01"}` | 200 OK | ✅ |
| `DELETE /api/v1/devices/{deviceId}` | 设备删除 | — | 200 OK / 204 No Content | ✅ |

### 4.2 设备控制接口

| 接口 | 验证内容 | 预期结果 | 平台调用点 | 状态 |
|------|----------|----------|-----------|------|
| `POST /api/v1/devices/{deviceId}/gate/open` | 开闸 | ACKNOWLEDGED（无MQTT真机时超时→UNCERTAIN） | 出场支付成功、人工开闸 | ✅ 接口通，超时预期 |
| `POST /api/v1/devices/{deviceId}/gate/close` | 关闸 | 闸杆落下，返回 200 + ACKNOWLEDGED | 运营后台手动关闸 | ☐ 真机联调 |
| `POST /api/v1/devices/{deviceId}/display/text` | 显示屏文字 | LED 屏显示指定内容 | 入场显示"欢迎光临"，出场显示收费金额 | ☐ 真机联调 |
| `POST /api/v1/devices/{deviceId}/display/config` | 显示屏配置 | 音量/亮度/方向更新生效 | 运营后台显示屏控制弹窗 | ☐ 真机联调 |
| `POST /api/v1/devices/{deviceId}/voice/control` | 语音播报 | 喇叭播放指定语音 | 出场时播报收费金额 | ☐ 真机联调 |
| `POST /api/v1/devices/{deviceId}/time/sync` | 校时 | 设备时间同步为服务器时间 | 设备管理页校时按钮 | ☐ 真机联调 |
| `GET /api/v1/devices/{deviceId}/status` | 状态查询 | 返回在线/离线状态 | 设备管理页状态显示 | ☐ 真机联调 |

### 4.3 事件推送接口

| 接口 | 验证内容 | 推送方式 | 预期结果 | 平台处理 | 状态 |
|------|----------|---------|----------|---------|------|
| `POST /api/v1/device-webhook/events` | 车牌识别事件 (ENTRY) | Device Access → 平台 HTTP POST | 平台创建 ParkingSession，状态=IN | 设备身份校验→车道方向匹配→创建Session→开闸调用→UNCERTAIN告警 | ✅ |
| `POST /api/v1/device-webhook/events` | 车牌识别事件 (EXIT) | Device Access → 平台 HTTP POST | 平台匹配在场记录，BillingEngine 计费，状态→OUT | 匹配Session→BillingEngine计算(首小时5元)→计费结果返回→状态流转→开闸调用 | ✅ |
| `POST /api/v1/device-webhook/events` | 重复 eventId | Device Access 重复推送 | 平台幂等忽略，不重复创建记录 | 返回 200，业务忽略，processedEvents 内存去重 | ✅ |
| `POST /api/v1/device-webhook/events` | 未知 deviceSn | Device Access 推送未知设备 | 平台记录错误日志，返回 200 | 不处理，不抛异常 | ✅ |

### 判断标准

- **通过**：每个接口返回预期状态码和字段值
- **部分通过**：核心控制接口（开闸/关闸/识别事件）通过，辅助接口可后续补测
- **失败**：核心接口返回 5xx、超时或数据不一致，需排查

---

## 5. P云支付联调项

| 检查项 | 验证内容 | 请求/触发方式 | 预期结果 | 状态 |
|--------|----------|-------------|----------|------|
| `POST /api/v1/mini/pay/prepare` | 预下单 | 岗亭端/小程序提交支付请求 | 返回 prepayParams（含 prepayId） | ⏳ mock模式 |
| 被扫支付 | 岗亭扫码枪扫描付款码 | 岗亭端输入付款码 → 调 P云 API | 支付成功 | ⏳ mock模式 |
| `POST /api/v1/mini/pay/notify` | 支付回调通知 | P云异步回调平台 | 订单状态更新为 PAID | ✅ SQL模拟验证通过 |
| 回调幂等 | 重复回调 | 同一 notify 重复发送 | 订单状态不重复更新 | ✅ SQL模拟验证通过 |
| 回调验签 | 非法回调 | 伪造回调请求 | 验签失败，拒绝处理 | ✅ 已知验签逻辑就绪 |
| WebSocket 推送 | 支付成功通知岗亭端 | 支付回调处理完成后推送 | 收费面板更新为"已支付，请放行" | ⏳ 未验证 |
| 退款 | 退款操作 | 运营后台发起退款 | 退款成功，订单状态更新 | ☐ P云联调 |

### 判断标准

- **通过**：预下单返回合法参数，支付回调正常处理，验签和幂等正确
- **部分通过**：基础支付流程通过，幂等/退款可后续补测
- **失败**：支付回调无法正常处理或状态更新不一致

---

## 6. 端到端场景

> 验证日期：2026-07-15 | 环境：Docker（无真机 MQTT） | 测试车辆：京 PAY001

### 6.1 临时车入场 ✅

| 步骤 | 操作 | 预期结果 | 验证结果 |
|------|------|----------|---------|
| 1 | Device Access 推送入口车牌识别事件（ENTRY） | 平台收到 Webhook | ✅ curl 模拟推送成功 |
| 2 | 平台 DeviceWebhookController 接收并处理事件 | 记录日志 | ✅ 身份校验通过，direction 匹配 |
| 3 | 平台创建 ParkingSession（状态=IN） | 数据库插入成功 | ✅ sessionId 正确，entry_time 正常 |
| 4 | 平台调用 DeviceAccessClient.openGate() 开闸 | 返回 CommandResultDTO | ✅ ACKNOWLEDGED（MQTT 超时预期） |
| 5 | 闸杆抬起 | 物理闸杆动作 | ⏳ 无真机，超时预期 |
| 6 | 岗亭 WebSocket 收到入场通知 | 监控大屏显示入场记录 | ⏳ 未验证 |
| 7 | 车位余量 -1 | 数据库余量字段更新 | ⏳ 未验证 |

**验证点**：Session 创建 ✅、开闸调用 ✅、UNCERTAIN 告警 ✅

### 6.2 临时车出场缴费 ✅

| 步骤 | 操作 | 预期结果 | 验证结果 |
|------|------|----------|---------|
| 1 | Device Access 推送出口车牌识别事件（EXIT） | 平台收到 Webhook | ✅ curl 模拟推送成功 |
| 2 | 平台匹配在场记录，BillingEngine 计费 | 费用与计费规则一致 | ✅ 首小时 5.00 元（feeCents=500） |
| 3 | 生成待支付订单 | 订单状态=PENDING | ✅ SQL 模拟 |
| 4 | 岗亭收费面板弹出，显示车牌/时长/金额 | 收费面板数据正确 | ⏳ UI 联调 |
| 5 | 岗亭操作员扫码（被扫模式） | 支付成功 | ⏳ P云 mock 模式 |
| 6 | P云回调平台通知 | 订单状态更新为 PAID | ✅ SQL 模拟验证，对账一致 |
| 7 | 平台调用 DeviceAccessClient.openGate() 开闸 | 闸杆抬起 | ✅ ACKNOWLEDGED（超时预期） |
| 8 | 停车记录状态更新为 EXITED | 数据库更新成功 | ✅ status=OUT，exit_time 正确 |
| 9 | 车位余量 +1 | 数据库余量字段更新 | ⏳ 未验证 |
| 10 | 岗亭 WebSocket 收到支付成功通知 | 收费面板显示已放行 | ⏳ 未验证 |

**验证点**：费用计算 ✅、订单创建 ✅、支付状态流转 ✅、开闸调用 ✅、状态流转 ✅

### 6.3 月卡车入场 ☐

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | Device Access 推送入口车牌识别事件（ENTRY） | 平台收到 Webhook |
| 2 | 平台匹配月卡校验 | 月卡有效 |
| 3 | 平台创建 ParkingSession（状态=IN） | 数据库插入成功 |
| 4 | 平台开闸 | 闸杆抬起 |
| 5 | 岗亭 WebSocket 收到入场通知 | 显示"月卡车入场" |

**验证点**：月卡有效期校验、开闸、状态流转

### 6.4 月卡车出场 ☐

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | Device Access 推送出口车牌识别事件（EXIT） | 平台收到 Webhook |
| 2 | 平台匹配在场记录，校验月卡有效期 | 月卡有效，费用=0 |
| 3 | 开闸放行 | 闸杆抬起 |
| 4 | 停车记录状态更新为 EXITED | 数据库更新成功 |

**验证点**：月卡校验、免费放行、开闸、状态流转

### 6.5 人工放行 ✅

| 步骤 | 操作 | 预期结果 | 验证结果 |
|------|------|----------|---------|
| 1 | 岗亭操作员点击"人工放行"按钮 | 弹出原因选择窗口 | ⏳ UI 联调 |
| 2 | 选择原因（设备故障/VIP/紧急/其他） | 原因记录 | ⏳ UI 联调 |
| 3 | 确认放行 | 调用后端 manualOpenGate | ✅ 通过 executeGateOpen 验证开闸路径 |
| 4 | 后端调用 DeviceAccessClient.openGate() 开闸 | 闸杆抬起 | ✅ 接口调用通过（超时预期） |
| 5 | 记录完整审计日志（操作人、时间、原因、结果） | 数据库审计记录 | ✅ UNCERTAIN 告警自动创建验证通过 |
| 6 | 停车记录更新（如适用） | 记录人工放行标记 | ⏳ 二期完善 |

**验证点**：审计日志 ✅、开闸调用 ✅、UNCERTAIN 告警 ✅

---

## 7. 待确认事项

| 事项 | 影响范围 | 当前状态 | 需确认方 | 确认后操作 |
|------|---------|---------|---------|-----------|
| Device Access v0.4 服务实际 IP 地址 | Webhook 回调地址配置、平台 Client 连接地址 | 待确认 | Device Access 团队 | 更新 application.yml 中的 device-access.base-url 和 webhook.allowed-ips |
| P云 tradePrepare 返回的 JSAPI 参数字段名 | 小程序调起支付参数解析 | 待确认 | P云支付团队 | 更新 MiniPayController 解析逻辑 |
| 语音模板 ID 列表（voiceId → 内容映射） | 语音播报功能 | 待确认 | Device Access 团队 | 更新平台语音播报参数映射 |
| 显示屏配置参数范围（音量 0-100？亮度 0-255？） | 显示屏控制功能 | 待确认 | Device Access 团队 / 厂商文档 | 更新前端滑块范围和后端校验 |
| P云回调重试策略（重试次数、间隔、超时） | 支付回调幂等设计 | 待确认 | P云支付团队 | 确认无需额外重试防护 |
| Device Access Webhook 重试策略 | Webhook 推送可靠性 | 待确认 | Device Access 团队 | 确认是否需要处理重试 |
| 厂商开闸保持时长默认值（duration 参数） | 开闸调用参数 | 待确认 | Device Access 团队 | 更新默认值 |
| 联调环境网络策略（需要开放哪些端口/IP） | 环境部署 | 待确认 | 运维/网络团队 | 开通策略 |

---

## 8. 风险与兜底

| 风险 | 影响 | 概率 | 严重程度 | 兜底方案 |
|------|------|------|---------|----------|
| 开闸失败（设备离线/厂商异常） | 车辆无法出场 | 中 | 高 | 岗亭端人工放行兜底，自动创建异常告警 |
| 支付超时（P云接口响应慢） | 订单状态不一致 | 低 | 高 | 后端主动查询支付状态 + 人工确认 |
| Webhook 重复推送 | 重复创建停车记录 | 中 | 中 | 幂等校验（eventId + deviceSn + captureTime 唯一约束） |
| 设备离线（无心跳） | 无法识别/开闸 | 中 | 高 | 岗亭端手动输入车牌 + 人工开闸兜底 |
| 网络中断（平台 ↔ Device Access） | 控制指令超时 | 低 | 高 | 指令标记为 UNCERTAIN，通过状态查询确认设备状态 |
| P云回调丢失 | 订单一直为 PENDING | 低 | 高 | 定时任务主动查询超时未支付订单 + 人工对账 |
| Webhook IP 白名单配置错误 | Device Access 推送被拒绝 | 低 | 中 | 开发环境允许 `*`，生产环境配置准确 IP |
| SN 映射不一致 | 开闸调用失败 | 中 | 中 | 联调前核对平台台账 SN 与 Device Access 设备 SN 一致 |

### 兜底操作流程

#### 开闸失败兜底

```
开闸调用失败 / 超时
    ├── 返回 UNCERTAIN 状态
    ├── 创建异常告警（设备故障）
    ├── 停车记录保持 PAID（不变成 EXITED）
    └── 岗亭操作员可选择：
        ├── 再次尝试开闸（记录重试原因）
        └── 人工确认放行（记录操作人和原因）
```

#### 支付异常兜底

```
支付回调超时
    ├── 岗亭端显示"支付处理中"
    ├── 后端定时任务查询 P云支付状态
    ├── 付款码有效期内可重试预下单
    └── 人工确认支付后操作：
        ├── 在运营后台手动确认订单
        └── 确认后手动触发开闸
```

---

## 9. E2E 验证结论（2026-07-15）

### 验证摘要

| 维度 | 结果 |
|------|------|
| **识别→计费→收费→放行 核心闭环** | ✅ **全部通过** |
| 临时车入场流程 | ✅ Webhook ENTRY → 设备校验 → Session创建 → 开闸调用 → UNCERTAIN告警 |
| 临时车出场计费流程 | ✅ Webhook EXIT → Session匹配 → BillingEngine → feeCents=500 (首小时5元) → 状态OUT |
| 支付对账闭环 | ✅ 订单 PENDING→PAID，feeAmount=paidAmount=5.00，对账一致 |
| 开闸调用 | ✅ Device Access v0.4 gate/open HTTP 调用通过（MQTT无真机，超时行为符合预期） |
| UNCERTAIN 告警 | ✅ 开闸超时自动写入 monitor_alert，3种告警类型验证通过 |
| 幂等去重 | ✅ 重复 eventId 被 processedEvents 缓存正确过滤 |
| 未知设备处理 | ✅ 未知 deviceSn 记录日志，不抛异常，返回200 |

### E2E 关键数据

```
parking_session (京PAY001):
  fee_amount=5.00 | paid_amount=5.00 | status=OUT
  entry_time=13:51 | exit_time=13:53 | duration=~2min
  gateOpened=null（一期无法确认闸杆状态）

parking_order:
  amount_cents=500 | paid_amount=500 | status=PAID
  金额对账: feeAmount(5.00) == paidAmount(5.00) ✅

monitor_alert:
  type=DEVICE_FAULT | DEVICE_UNCERTAIN | DEVICE_CONFIG_MISSING
  severity=WARNING | 创建成功 ✅
```

### 已验证能力

| 模块 | 能力 | 验证方式 |
|------|------|---------|
| DeviceWebhookService | 设备身份校验（deviceSn→tenantId/lotId/laneId） | Webhook curl 模拟 |
| DeviceWebhookService | 方向匹配（ENTRY→车道 direction） | 空安全校验修复后通过 |
| DeviceWebhookService | 幂等去重（eventId 缓存） | 重复推送验证 |
| RecognitionEventServiceImpl | executeGateOpen（含 parkingLotId 参数） | 开闸调用验证 |
| BillingEngine | HOURLY 阶梯计费（首时段/后续时段） | 首小时5元→feeCents=500 |
| MonitoringAlertService | createGateAlert（3种类型） | UNCERTAIN/DEVICE_FAULT 验证 |
| DeviceAccessConfig | API Key 拦截器（X-API-Key header） | 通信链路通过 |
| GateOpenService | 开闸三层状态（gateCommandSent/gateDeviceAck/gateOpened） | gateOpened=null 确认 |

### Flyway 迁移补充（联调修复）

| 迁移文件 | 内容 | 状态 |
|---------|------|------|
| V20260809003__add_deleted_at_to_parking_record.sql | parking_record 添加 deleted_at | ✅ 已应用 |
| V20260809004__add_parking_record_id_to_parking_session.sql | parking_session 添加 parking_record_id | ✅ 已应用 |

### 已知限制

| 限制 | 说明 | 计划 |
|------|------|------|
| 开闸 MQTT 超时 | 无真机连接，DA 侧 MQTT 下发无响应 | 真机联调（V01-V04） |
| gateOpened=null | 一期无法确认闸杆实际状态 | v1.0 设备状态反馈 |
| 关闸 | 一期未实现 GateCloseService | 二期完善 |
| P云真支付回调 | mock 模式下通过 SQL 模拟验证 | 真回调联调 |
| 月卡车场景 | 月卡模块依赖未列入本次 Sprint | 后续 Sprint |
| WebSocket 推送 | E2E 未覆盖实时推送验证 | UI 联调覆盖 |
| 车位余量 | 未实现余量扣减/恢复 | 二期完善 |
| ParkingLane entity | system 模块 entity 字段未同步 DB 真实结构（lot_id/lane_no/type） | 后续 Sprint 对齐 |

---

## 签字确认

| 角色 | 签字 | 日期 |
|------|------|------|
| 后端负责人 | | |
| 前端负责人 | | |
| Device Access 负责人 | | |
| P云支付负责人 | | |
| 测试负责人 | | |
| 技术负责人 | | |

---

## 关联文档

- [联调前阻塞项检查清单](./联调前阻塞项检查清单.md) — 联调前必须通过的 P0 项
- [API 接口文档](./04-API接口文档.md) — 联调涉及的所有 API 接口
- [PRD V1.1](../需求文档/停车SaaS系统完整需求文档_PRD_V1.0_最终定稿.md) — 产品需求定义
- [AGENTS.md](../../AGENTS.md) — Device Access 冻结契约
