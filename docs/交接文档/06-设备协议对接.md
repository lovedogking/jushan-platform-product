# 06 — 设备协议对接

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                       EMQX (MQTT Broker)                     │
│                        端口: 1883                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ 臻识/C5  │  │  芊熠    │  │ 信路通   │                  │
│  │ 主题:    │  │ 主题:    │  │ 主题:    │                  │
│  │ zhenshi/ │  │ qianyi/  │  │ xinlutong│                  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘                  │
└───────┼─────────────┼─────────────┼──────────────────────────┘
        │             │             │
        ▼             ▼             ▼
┌───────────────────────────────────────────────────────────┐
│           device-access（独立 JVM 进程，端口 8082）          │
│  ┌─────────┐  ┌───────────┐  ┌────────────────┐          │
│  │adapter/ │  │event/      │  │registry/       │          │
│  │臻识适配 │  │事件引擎    │  │设备注册        │          │
│  │芊熠适配 │  │MQTT路由    │  │心跳管理        │          │
│  │信路通   │  │EventOutbox │  │状态快照        │          │
│  └─────────┘  └─────┬─────┘  └────────────────┘          │
│                      │                                     │
│                   Webhook (HMAC)                            │
│                  POST /api/v1/device-webhook/events         │
└──────────────────────┼──────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────┐
│                  parking-system (Spring Boot)               │
│  DeviceWebhookController → DeviceWebhookService             │
│  → persistEventLog() → handleRecognition()                 │
│  → RecognitionEventService.processEvent()                  │
└───────────────────────────────────────────────────────────┘
```

## 二、品牌适配详情

### 2.1 臻识（Zhenshi）— C5 相机

**通信协议**：MQTT
**设备 SN**：`15ZK231028466482`

**上行主题**：
- `zhenshi/{sn}/recognition` — 识别事件（含图片 OSS URL、车牌号、颜色、方向）
- `zhenshi/{sn}/heartbeat` — 心跳
- `zhenshi/{sn}/status` — 状态上报

**下行主题**：
- `zhenshi/{sn}/command` — 通用命令通道

**核心功能**：

| 功能 | 实现 | 说明 |
|---|---|---|
| 识别接收 | ✅ | MQTT 消息解析 → 事件对象 → EventOutbox |
| OSS 图片下载 | ✅ | `RemoteImageDownloader` 异步下载 OSS URL 到本地，每日 03:20 清理（保留 30 天） |
| 主动抓拍 | ✅ (v0.7.1) | MQTT snapshot 两段式：下发 snapshot cmd → 等设备回复 → 再等识别事件带图片 URL → OSS 下载落盘 |
| 开闸/关闸 | ✅ | MQTT 下行控制命令 |
| 常开锁定 | ✅ | 特殊命令帧 |
| 语音播报 | ✅ | 下行语音命令（音量 + 男女声 + 文本） |
| 显示屏 | ✅ | 下行显示命令（文字 + 颜色 + 旋转 + 亮度） |
| 时间同步 | ✅ | 每日 2:00 AM cron 定时下发 |
| 白名单同步 | ⚠️ | 后端已实现 sync，DA 侧仅打日志（白名单协议未实现） |
| 设备重启 | ⚠️ | 后端已实现 reboot 接口，DA 侧返回 404（协议未实现） |
| 触发识别 | ⚠️ | 后端已实现 trigger 接口，DA 侧返回 404 |

### 2.2 芊熠（Qianyi）

**通信协议**：MQTT + HTTP
**设备 SN**：`917e2298-8ddf3e46`

**上行主题**：
- `qianyi/{sn}/register` — 设备注册
- `qianyi/{sn}/heartbeat` — 心跳
- `qianyi/{sn}/recognition` — 识别事件
- `qianyi/{sn}/iooutput` — 开闸状态反馈

**核心功能**：

| 功能 | 实现 | 说明 |
|---|---|---|
| 设备注册 | ✅ | 自动注册，SN 小写规范化 |
| 心跳管理 | ✅ | 状态更新 + 设备管理 |
| 识别接收 | ✅ | MQTT 解析，含车牌/颜色/方向 |
| 图片上传 | ✅ | 独立 HTTP：`POST /api/v1/images/upload`，Nginx 反代到 DA |
| 开闸/关闸 | ✅ | MQTT 下行 |
| 常开 | ✅ | MQTT 下行 |
| 对时 | ✅ | NTP 协议同步 |
| RS485 透传 | ✅ | OLM-M1D 显示屏/语音模块，0x30/0x31 协议 |
| 主动抓拍 | ✅ | snapshot 优先，tarkphoto 回退 |

### 2.3 信路通（Xinlutong）

**状态**：⚠️ 框架已搭建，适配未完成

**已实现**：
- MQTT 监听 `xinlutong/#` 主题
- `XinlutongDeviceCoordinator` 骨架
- 设备注册框架

**未实现**：
- 识别事件解析
- 图片处理
- 主动抓拍（`capture` 方法为桩）
- 命令下发

---

## 三、Webhook 协议

### 签名方式

```java
// HMAC-SHA256
String sign = HmacUtils.hmacSha256Hex(webhookSecret, requestBody);
// 验证: 请求头 X-Signature == sign
```

### URL

```
POST /api/v1/device-webhook/events
Headers:
  X-Signature: <HMAC-SHA256签名>
  X-Device-SN: <设备SN>
  X-Event-Type: recognition / heartbeat / status
Body: JSON (原始设备事件)
```

### 事件映射

`DeviceWebhookController.mapToEvent()` 将 DA 推送的原始事件映射为 `DeviceWebhookEvent` 对象，含：
- 车牌号（大写）
- 车牌颜色
- 车辆类型（相机识别结果，后续被系统判定引擎覆盖）
- 车身颜色（bodyColor，V20260906015）
- 车标（carLogo，V20260906015）
- 方向（entry/exit）
- 图片 URL

---

## 四、识别联动模板

设备表（device）中的 6 个模板字段：

| 字段 | 场景 | 默认值 |
|---|---|---|
| `voice_entry_welcome_template` | 入场语音欢迎 | `入口欢迎{type}车牌{plate}` |
| `voice_exit_welcome_template` | 出场语音 | `出口再见{type}车牌{plate}` |
| `voice_forbidden_template` | 禁止入场语音 | `禁止入场{type}车牌{plate}` |
| `display_entry_welcome_template` | 入场显示屏 | 同上 |
| `display_exit_welcome_template` | 出场显示屏 | 同上 |
| `display_forbidden_template` | 禁止入场显示屏 | 同上 |

占位符 `{plate}` → 车牌号，`{type}` → 车辆类型中文标签。

前端编辑时自动隐藏前缀 `{plate},{type},`，预览时拼接示例值。

---

## 五、设备 SN 清单

| SN | 品牌 | 型号 | 备注 |
|---|---|---|---|
| `15ZK231028466482` | 臻识 | C5 | 开发调试用 |
| `917e2298-8ddf3e46` | 芊熠 | QY-Q3 | 开发调试用 |
| `16ZD291949576572` | 臻识 | — | 在线 |
| `16ZD301554180962` | 臻识 | — | 在线 |

> 后两台为服务器上实际在线的额外设备（通过 DeviceAccessClient 心跳检测确认在线）。

## 六、device-access 数据库

device-access 有自己的独立 MySQL 数据库 `device_access`（端口 3307），与 parking-boot 的 `jushan_platform` 完全隔离。主要表：

| 表名 | 说明 |
|---|---|
| `t_device_product` | 设备产品型号（品牌、型号名、device_type、protocol、capabilities JSON） |
| `t_device` | 设备实例（SN、product_id、状态） |
| `t_event_outbox` | 事件发件箱（识别事件持久化 + 重试，Webhook 推送前的中间队列） |

> 数据库连接信息见 DA 的 `application-prod.yml`，用户名/密码与 parking-boot 的 MySQL 相同（`jushan` / `8Lkohb4oMzkmfaro554XaZbW6O4=`）。

## 七、已知 DA 缺口

| 功能 | parking-system 接口 | DA 侧状态 | 处理 |
|---|---|---|---|
| 设备重启 | `POST /devices/{sn}/reboot` | 返回 404 | 后端捕获，返回友好提示 |
| 触发识别 | `POST /devices/{sn}/trigger` | 返回 404 | 同上 |
| 白名单同步 | `POST /devices/{sn}/whitelist` | 仅打日志 | 同上 |
| 信路通全部功能 | — | 骨架 | 需完整适配开发 |
