# 08 — 已知问题与风险

> 按优先级排列，标注影响面和解决建议。

---

## P0 — 阻塞二期或可能导致线上问题

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| P0-1 | **计费体系二选一**：旧 `BillingRule`（线上在用） vs 新 `FeeRule`（冻结）并存，无统一入口 | P0-1 计费规则引擎启动前必须决定迁移/切换策略，否则两套并行维护成本极高 | 决策：废弃旧 BillingRule，解冻 FeeRule 并补前端运营端配置页 + 按车型维度定价 |
| P0-2 | **微信支付服务商模式未实现**：当前仅 P云 对接 + 模拟支付，代码无任何微信支付直连接入 | 阻塞 P0-4 微信支付上线 | 需微信商户号资质申请（外部依赖，周期长）+ 支付渠道抽象层 + 服务商模式开发 |
| P0-3 | **人工补录入场缺权限**：`GAP-08`，`BoothManualEntryController` 无 `@RequirePermission` | 任何登录用户都能调 | 补权限注解 |
| P0-4 | **跟车处理未实现**：待核实代码是否存在对应处理逻辑 | 一车一杆场景下的安全漏洞 | 需按设备协议确认跟车检测能力 |
| P0-5 | **改价功能未实现**：需独立权限 + 操作日志 | 岗亭收费场景缺失 | 规格化后实现 |
| P0-6 | **对账完全空白**：无支付流水 vs 订单自动对账 | 财务合规风险 | P0-6 对账模块 + xxl-job 定时任务 |

## P1 — 重要但可延后

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| P1-1 | **xxl-job 未引入**：月卡到期提醒、超时黑名单、对账都需要定时任务 | 需基础设施决策 | 引入 xxl-job 或直接用 Spring @Scheduled |
| P1-2 | **小程序前端不存在**：`miniapp/` 目录为空，后端接口已完备 | P2 小程序启动需从零建工程 | 评估用 uni-app 还是原生微信小程序 |
| P1-3 | **月卡到期自动化**：到期转临停计费、宽限期、到期前 N 天提醒均未实现 | 月卡商业化完成度 | 二期 P1-3 实现 |
| P1-4 | **信路通设备适配**：`XinlutongDeviceCoordinator.capture` 为桩 | 引入信路通设备时需完成适配 | 按需开发 |
| P1-5 | **DA 白名单协议**：`syncWhitelist` 目前只打日志，未真正下发到相机 | 离线识别场景不工作 | 需确认臻识/芊熠白名单协议后实现 |
| P1-6 | **DA 重启/触发识别**：parking-system 接口已完备，DA 返回 404 | 运营端设备管理功能不完整 | DA 侧实现对应端点 |

## P2 — 改善项

| # | 问题 | 说明 |
|---|---|---|
| P2-1 | 岗亭断网本地开闸 + 记录补传仅前端离线队列 | 极端场景未覆盖 |
| P2-2 | 旧租户体系双轨：`tenant` + `registration` vs `sys_tenant` 并存 | 旧审批流需迁移或下线 |
| P2-3 | 前端 TypeScript 类型覆盖不完整 | 部分 API 调用使用 `any` |
| P2-4 | 后端单元测试缺失 | 仅少量测试 |
| P2-5 | 异常分类不完备 | 部分业务异常使用通用 BusinessException |
| P2-6 | 前端无 CI/CD pipeline | 每次手动 vite build + rsync 部署 |

---

## 三、技术债清单

| 项 | 位置 | 说明 |
|---|---|---|
| 新旧计费规则并存 | `parking/parking-service` | 需清理旧代码 |
| `com.jushan.system.*` 遗留 | 全系统 | 仅剩 `entity/Company.java`，新代码需规范到 `modules/` |
| Mapper 直接继承 `ServiceImpl` 的旧写法 | 部分遗留模块 | 需迁移到 Service 接口 + impl 模式 |
| Flyway 迁移 V20260906015 | `recognition_event_log` 表 | checksum 曾经修复（手动修改 SQL 后 `SET checksum=NULL`） |
| 前端全局样式 `global.scss` | 前端 | 存在部分直接覆盖 ant-design 默认样式，升级 UI 库需注意 |
| `std::async` vs `std::thread` 无显式保障 | device-access 图片下载 | 改为自建线程池可更稳定 |
| RemoteImageDownloader 线程安全 | device-access | 多设备并发下载时需注意 OSS 连接池 |
