# 07 — 前端开发指南

## 一、项目结构

```
frontend/
├── index.html
├── vite.config.ts          Vite 配置（含代理）
├── tsconfig.json
├── src/
│   ├── main.ts             应用入口
│   ├── App.vue
│   ├── api/                API 封装层（按模块分文件）
│   │   ├── account.ts      账号管理
│   │   ├── auth.ts         鉴权
│   │   ├── charge.ts       收费
│   │   ├── fee-rule.ts     计费规则
│   │   ├── monitor.ts      岗亭监控
│   │   ├── monitor-types.ts类型定义
│   │   ├── operation-log.ts操作日志
│   │   ├── parking-lot.ts  车场查询
│   │   ├── parking-manage.ts车场管理
│   │   ├── vehicle.ts      车辆管理
│   │   └── vehicle-query.ts车牌查询
│   ├── components/         业务组件
│   │   ├── ChargePanel.vue         收费面板
│   │   ├── FeeRuleEditModal.vue    计费规则编辑弹窗
│   │   ├── ManualReleaseModal.vue  人工放行弹窗（640px，左右布局+虚拟键盘）
│   │   ├── PlateCorrectionModal.vue车牌校正弹窗
│   │   ├── PlateTag.vue            车牌标签（5种颜色自动识别）
│   │   ├── SpaceAdjustModal.vue    余位调整弹窗
│   │   └── TempPlateAlertModal.vue 临时车牌告警弹窗
│   ├── composables/        组合式函数
│   │   ├── useChart.ts     ECharts 图表封装
│   │   └── useNetworkStatus.ts 网络状态检测
│   ├── layout/
│   │   └── UnifiedLayout.vue       统一布局（侧边栏+顶栏）
│   ├── router/
│   │   └── index.ts               路由配置
│   ├── stores/
│   │   └── monitor.ts             WebSocket 状态管理（Pinia）
│   ├── styles/
│   │   ├── global.scss            全局样式
│   │   └── variables.scss         SCSS 变量
│   ├── utils/
│   │   ├── request.ts             Axios 封装（拦截器/JWT/错误处理）
│   │   ├── websocket.ts           WebSocket 客户端（自动重连）
│   │   └── offline-queue.ts       离线请求队列（localStorage 暂存）
│   └── views/
│       ├── login/
│       │   └── index.vue           登录页
│       ├── admin/
│       │   └── Accounts.vue        管理账号页
│       ├── error/
│       │   └── 404.vue             404 页面
│       ├── monitor/
│       │   ├── index.vue           岗亭工作区（四宫格+右侧栏）
│       │   ├── MonitorTabs.vue     监控标签页
│       │   ├── ParkingLotSidebar.vue车场侧边栏
│       │   └── VehicleQuery.vue    车辆查询
│       └── operation/
│           ├── Dashboard.vue       运营仪表盘
│           ├── ParkingManage.vue   停车场管理（基本信息/车道/设备）
│           ├── Vehicles.vue        车辆列表
│           ├── FeeRules.vue        计费规则
│           ├── AccessRecords.vue   通行记录
│           ├── OperationLogs.vue   操作日志
│           ├── Analytics.vue       数据分析
│           └── components/
│               ├── DeviceFormFields.vue  设备表单字段
│               ├── DeviceManager.vue     设备管理面板（三标签：识别联动/设备参数/手动测试）
│               ├── LaneManager.vue       车道管理
│               └── LotBasicInfo.vue      车场基本信息
```

## 二、路由结构

| 路径 | 组件 | 说明 |
|---|---|---|
| `/login` | views/login/index.vue | 登录页 |
| `/booth` | views/monitor/index.vue | 岗亭工作区 |
| `/operation` | views/operation/Dashboard.vue | 运营首页 |
| `/operation/parking-manage` | views/operation/ParkingManage.vue | 停车场管理 |
| `/operation/vehicles` | views/operation/Vehicles.vue | 车辆管理 |
| `/operation/fee-rules` | views/operation/FeeRules.vue | 计费规则 |
| `/operation/access-records` | views/operation/AccessRecords.vue | 通行记录 |
| `/operation/operation-logs` | views/operation/OperationLogs.vue | 操作日志 |
| `/operation/analytics` | views/operation/Analytics.vue | 数据分析 |
| `/admin/accounts` | views/admin/Accounts.vue | 管理账号 |
| `/404` | views/error/404.vue | 404 页面 |

> 路由配置中的 `meta.requiresAuth` 控制是否需要登录；岗亭端 `/booth` 需要 `userType = booth` 或平台用户；运营端 `/operation` 需要平台或租户管理员角色。

## 三、API 调用模式

```typescript
// 1. 定义类型（api/xxx-types.ts 或同文件中）
interface VehicleVO {
  id: number
  plateNumber: string
  vehicleType: string
  // ...
}

// 2. 封装 API（api/vehicle.ts）
/** 查询车辆 */
export function queryVehicle(plateNumber: string): Promise<VehicleVO | null> {
  return request.get(`/v1/vehicles/query`, { params: { plateNumber } })
}

// 3. 在组件中使用
import { queryVehicle } from '@/api/vehicle'
const v = await queryVehicle('川A88888')
```

## 四、WebSocket 通信

```typescript
// stores/monitor.ts
import { useWebSocket } from '@/utils/websocket'

// 连接与订阅
const ws = useWebSocket('/ws/booth')

// 监听消息
ws.onMessage((data) => {
  if (data.type === 'space_update') {
    remainingSpaces.value = data.payload
  }
})
```

## 五、关键组件说明

### PlateTag.vue
根据车牌号自动判断颜色：绿牌（新能源，7-8位第三位为字母）、黄牌（尾字母或首字母）、黑牌、白牌、蓝牌。判断逻辑在 `detectPlateColor()` 中。

### ManualReleaseModal.vue
人工放行弹窗，640px 宽，左右布局。使用 `KeyboardEvent` 虚拟键盘输入车牌。`queryVehicleType()` 在车牌输入超过 6 位时自动调用后端匹配车辆类型。入场/出场表单通过 `v-if="formState.direction==='exit'"` 区分计费区域。

### ChargePanel.vue
出场收费面板，从 Props 接收 `ParkingSessionVO`，显示入场时间、时长、费用。确认收费调用模拟支付接口→开闸→关闭面板。

## 六、开发注意事项

- **金额**：后端返回分，前端显示时 `/100` 转为元
- **路径前缀**：所有 API 经 `request.ts` 自动加 `/api`
- **权限拦截**：`@RequirePermission` 由后端校验，前端暂无按钮级权限控制
- **ant-design-vue**：按需引入，避免全量引入增加打包体积
- **响应拦截**：Axios 拦截器自动处理 `R<T>` 包装，code=200 时返回 `data.data`，非 200 时弹 toast 提示
