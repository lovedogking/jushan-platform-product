cmake_minimum_required(VERSION 2.8)

SET(BUILD_PROJECT_NAME "json")
SET(LIBRARY_OUTPUT_PATH  ${PROJECT_BINARY_DIR})

AUX_SOURCE_DIRECTORY(. SRC_LIST)
ADD_DEFINITIONS(-fPIC)
ADD_LIBRARY(${BUILD_PROJECT_NAME} STATIC ${SRC_LIST})
