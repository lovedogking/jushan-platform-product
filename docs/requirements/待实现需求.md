# 待实现需求清单（二期）

> **最近更新**：2026-08-11（同步计费现状：识别链路与 H5 已切换 FeeCalculationService；原 `提示词.md` 已删除，相关引用已改写）
> **维护规则**：见 [`AGENTS.md`](../../AGENTS.md) 第四节。需求完成 / 范围变更 / 优先级调整时**必须同次更新本文件**并刷新日期；完成的条目移入 [`已实现需求.md`](已实现需求.md)。
> **范围来源**：原根目录 `提示词.md`（已删除）。未列出的功能一律不进二期。
> **前置交付物**（尚未产出）：《一期功能现状清单》→ 人确认 →《二期需求规格文档》（含接口定义、DDL 规划、AC 验收清单）。产出后在此登记链接。

状态图例：◻️ 未启动 ｜ ◧ 部分实现（附现状引用，开发 = 补差而非从零）

---

## P0 计费与支付

| # | 需求 | 状态 | 现状 / Gap |
|---|---|---|---|
| P0-1 | 计费规则引擎：按车型/时段计费，免费时长、首小时、阶梯、每日封顶、跨天规则；按停车场维度可配置；运营端配置页面；规则版本与快照 | ◧ | `FeeRuleController` / `FeeRuleService` / `FeeCalculationService` CRUD 与试算已启用，运营端配置页 `FeeRules.vue` 与车场档案绑定 `fee_rule_id` 已存在。**A1/A2/A3/A4/A5 已完成**：`fee_rule` 已补 4 列；版本历史表 `fee_rule_history` + 回退接口；计算逻辑补全（首时段/maxAmount/nightCap/跨天模式/priority/车型/颜色/区域过滤）；`parking_session` 已加 `fee_rule_id`/`fee_rule_snapshot` 并在入场时写入快照；识别链路（`EntryService`/`ExitService`/`RecognitionEventServiceImpl`/`TempPlateService`）+ H5 查费/支付已切换为 `FeeCalculationService`。**剩余 Gap**：⑥ ≥10 个计费测试用例；⑦ 旧 `BillingRule` 及 `BillingEngine` 删除；⑧ `ParkingFeeService`/`MiniPayController`/`PyunPpFrontController` 等剩余 `BillingEngine` 引用清理。详见 [`二期实施计划.md`](二期实施计划.md) A 线 |
| P0-2 | 临时车全流程：识别入场→计费→出场缴费→开闸 | ◧ | 主链路已通（RecognitionEventService → FeeCalculationService → 开闸）。**Gap**：全流程联调验收 + 异常分支 |
| P0-3 | 异常场景：无牌车、识别纠错、跟车、断网兜底 | ◧ | 无牌车（temp-plate）、识别纠错已有；**跟车处理未见实现（待核实）**；断网仅前端离线请求重放，**本地开闸 + 恢复后补传记录未实现（二期纳入 P0，见 [`二期实施计划.md`](二期实施计划.md) C2）** |
| P0-4 | P云 OpenAPI 支付接入：H5 真实支付、P云 回调幂等、商户配置管理；保留模拟支付开关 | ◧ | H5 查费/支付后端（`modules/h5`）与前端（`h5-user/`）已搭骨架并通模拟支付；`PyunPpFrontController` 已标 `@Deprecated`。**Gap**：① ~~`H5FeeController`/`H5PayController` 切到 `FeeCalculationService`~~（已完成，B1）；② `H5PayController.prepare` 调 P云 OpenAPI 下单并返回 `pay_url`；③ 前端 `confirm.vue` 真实支付分支跳转 P云；④ 新增 P云 HTTP 回调接口并做幂等；⑤ 复活 `PyunPpFrontController` 并切计费入口；⑥ `PayMerchantConfig` 运营端配置页。详见 [`二期实施计划.md`](二期实施计划.md) B 线 |
| P0-5 | 岗亭收费：金额确认、改价（需权限+操作日志）、现金收费登记、远程开闸；岗亭操作全部留日志 | ◧ | 收费确认/现金（交班汇总）/远程开闸/费用减免已有；**改价（独立权限+日志）未见实现（待核实）**；操作日志已有 audit_log 基础，需按规格补齐覆盖 |
| P0-6 | 对账：每日支付流水与订单自动对账，对不上产生告警 | ◻️ | 无任何对账实现；采用 Spring Scheduler 每日任务（不用 xxl-job）。`monitor_alert` 表可作告警载体 |

## P1 月卡商业化

| # | 需求 | 状态 | 现状 / Gap |
|---|---|---|---|
| P1-1 | 月卡产品：月卡/季卡/年卡，绑定车牌，支持固定车位 | ◧ | 月卡 CRUD/审批、固定车位绑定已有；**季卡/年卡产品化是否支持待核实**（已有月卡价格参数） |
| P1-2 | 运营端开卡收费 + 车主小程序自助续费 | ◧ | 后端接口齐全（MonthlyPassController、MiniMonthlyPassController、续费闭环 VehicleRenewalService）；**小程序前端不存在** |
| P1-3 | 到期管理：到期自动转临时计费、宽限期可配置、到期前 N 天提醒（Spring Scheduler 定时任务） | ◻️ | 仅「到期列表查询」已有；无自动转临停/宽限期/提醒实现。二期采用 Spring Scheduler，不引入 xxl-job |

## P2 小程序端（车主侧）

| # | 需求 | 状态 | 现状 / Gap |
|---|---|---|---|
| P2-1 | 小程序前端工程（车牌绑定、月卡状态查询与续费、停车记录查询） | ◻️ | `miniapp/` 目录为空；后端接口已有（见 [`已实现需求.md`](已实现需求.md) 十节）。H5 车主端（`h5-user/`）可作为小程序 UI 参考 |
| P2-2 | 临停缴费：输车牌查费→支付→缴费后 N 分钟内免费出场（N 可配置） | ◧ | 查费（WxParkingFeeController）、预下单/通知已有；pay_window 表结构已有，**N 可配置与全流程验收待规格化** |

## P3 经营数据

| # | 需求 | 状态 | 现状 / Gap |
|---|---|---|---|
| P3-1 | 运营端日报/月报：收入（现金/线上分列）、车流量、临停/月卡占比 | ◧ | ReportController 营收/车流报表+导出已有；**现金/线上分列、临停/月卡占比是否满足规格待核实** |
| P3-2 | 余位统计 | ✅ | 余位计算/展示已有，可直接验收 |
| P3-3 | 运营端首页 dashboard | ◧ | DashboardController + AnalyticsController 基础版已有，按规格补差 |

---

## 明确不做（二期范围外）

> 以下来自原 `提示词.md`（已删除）的明确排除项。**任何方案不得将其纳入二期**；若要启动须先更新本节并移出。

- 优惠券
- 商户减免
- 多停车场集团化
- 充电桩
- 车位预约
- 支付宝（仅预留支付渠道可插拔接口）
- ETC

## 遗留风险与依赖

| 项 | 说明 | 影响 |
|---|---|---|
| P云 商户号/接口权限申请 | P云 OpenAPI 与 PP 体系接入资质（外部依赖） | 阻塞 P0-4 上线时间，需尽早启动 |
| 计费 runtime 双轨 | 识别链路与 H5 已切换 `FeeCalculationService`；旧 `BillingRule` 仅剩 `ParkingFeeService`/`MiniPayController`/`PyunPpFrontController`/`BillingRuleController` 引用 | 已决策：迁移至 `FeeRule` 并删除 `BillingRule`（A6 收尾），详见 [`docs/adr/0001-fee-rule-migration.md`](../adr/0001-fee-rule-migration.md) |
| P云对接去留 | 已决策生产环境走 P云 OpenAPI 纯支付；`PyunPpFrontController`（PP 体系）复活后用于停车场侧被 P云 调用 | 详见 [`二期实施计划.md`](二期实施计划.md) B 线 |
| 断网本地开闸 | 需要硬件/离线方案评估（相机本地白名单？岗亭本地服务？） | P0-3 方案设计前置 |
| xxl-job 引入 | 二期明确不引入 xxl-job；采用 Spring Scheduler 实现到期提醒与对账 | 基础设施决策已确定 |
| 信路通相机主动抓拍 | `XinlutongDeviceCoordinator.capture` 仍为桩；臻识（v0.7.1，MQTT snapshot 两段式 + OSS 下载落盘）与芊熠已实现 | 仅当引入信路通设备时需要 |
| 租户体系双轨 | 旧 `tenant`+`registration`（注册审批/配额模型，`TenantService`）与新 `sys_tenant` 并存；业务校验已统一切换 sys_tenant，旧 CRUD 无前端入口呈休眠 | 需决策旧审批流迁移或下线；原 max_employees 等配额检查已移除，待套餐体系重实现 |

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-07-23 | 首版：依据原 `提示词.md`（已删除）二期范围 + 代码索引 Gap 分析建立 |
| 2026-07-24 | 新增遗留项：臻识相机抓拍未实现、租户体系双轨待收敛（ParkingLotService/EmployeeService 已切 sys_tenant，旧配额检查移除） |
| 2026-07-24 | 臻识抓拍实现（MQTT snapshot 两段式 + OSS 下载落盘，device-access 迁移 V20260724002）；遗留项收窄为「信路通抓拍」 |
| 2026-07-27 | 新增 [`二期实施计划.md`](二期实施计划.md)；明确 P0-1 范围扩展为 FeeRule 完整化（含版本/快照）；P0-4 改为 P云 OpenAPI；P0-3 断网本地开闸纳入；明确不引入 xxl-job |
