# 已实现需求清单

> **最近更新**：2026-07-25（v1.6：芊熠 Q3 MQTT base64 图片直传；Webhook URL 修正；API Key 配置统一到 .env.production）
> **维护规则**：见 [`AGENTS.md`](../../AGENTS.md) 第四节。功能交付 / 状态变化时**必须同次更新本文件**并刷新日期。
> **数据来源**：本文档由 [`docs/code-index/`](../code-index/README.md)（代码级索引）归纳而成；类/方法/接口细节一律回查代码索引，不在此重复。

状态图例：✅ 正式可用 ｜ 🧪 占位实现 ｜ ⚠️ 已冻结（代码在但未启用）｜ 🚧 半成品

---

## 一、平台与多租户

| 功能 | 入口（接口基础路径 / 页面） | 主要数据表 | 状态 |
|---|---|---|---|
| 登录鉴权（后台/岗亭/小程序三端登录、改密） | `/auth`、`/api/v1/mini`（MiniAuthController） | sys_admin_account、sys_login_log | ✅ |
| 管理账号 CRUD、密码重置 | `/api/v1/admin-accounts` | sys_admin_account | ✅ |
| 自定义角色 + 权限矩阵 | `/api/v1/custom-roles` | sys_role、sys_role_permission | ✅ |
| 租户 CRUD 与审核 | `/api/admin/tenants` | tenant、registration | ⚠️ 旧体系休眠（业务校验已切 `sys_tenant`，无前端入口） |
| 公司/集团 CRUD + 树形 | `/api/admin/companies` | company | ✅ |
| 部门管理 | `modules/department` | sys_department | ✅ |
| 员工 CRUD / 密码重置 / 启停 | `/api/admin/employees` | employee | ✅ |
| 高风险操作审计日志 | `/api/admin/audit-logs` | audit_log | ✅ |
| 系统参数（分组/更新） | `/api/v1/system-params` | sys_config | ✅ |
| 车场级参数（列表/更新/重置） | `/api/admin/parking-lots/{lotId}/params` | parking_lot_param | ✅ |
| 文件上传 | `/api/v1/admin/files` | — | ✅ |

> 代码索引：`modules-account.md`、`modules-company.md`、`modules-department.md`、`modules-auth.md`、`modules-tenant.md`、`system-controller.md`

## 二、车场与设备档案

| 功能 | 入口 | 主要数据表 | 状态 |
|---|---|---|---|
| 停车场 CRUD / 状态 / 容量 / 就绪检查 | `/api/admin/parking-lots` | parking_lot | ✅ |
| 车道 CRUD / 状态管理 | `/api/admin/lanes` | parking_lane | ✅ |
| 停车区域 CRUD（标签：普通/VIP/员工/装卸/充电） | `/api/v1/parking-zones` | parking_zone | ⚠️ 冻结（二期计费候选） |
| 设备台账 CRUD / 状态快照 / 可用设备 | `/api/admin/devices` | device_vendor_model、device_status_snapshot | ✅ |
| **车道控闸设备显式指定（gate_device_id）** + **型号级能力默认（device_model.capabilities）** | Entity/ParkingLane / Entity/DeviceModel | parking_lane.gate_device_id、device_model.capabilities + 型号种子 Q3/C5 | ✅ |
| 车道-设备绑定 | （车场档案内） | lane_device_binding | ✅ |
| 设备指令审计 | — | device_command_audit | ✅ |
| Webhook 验签 | `modules/device/webhook` | webhook_secret | ✅ |

> 代码索引：`system-controller.md`、`modules-parking.md`、`modules-device.md`

## 三、设备接入（device-access 服务）

| 功能 | 说明 | 状态 |
|---|---|---|
| 平台-适配器-设备三层架构 | 7 个 Maven 子模块（api/common/adapter/event/mqtt/registry/starter） | ✅ |
| 臻识（C5）相机适配 | MQTT 上下行；OSS 限时图片 URL 事件到达即转存本地（RemoteImageDownloader） | ✅ |
| 信路通相机适配 | MQTT 协议适配 | ✅ |
| 芊熠相机适配 | 注册/心跳/识别/iooutput/常开/对时/RS485；SN 小写规范化；独立图片上传（`/api/v1/images/upload`） | ✅ |
| 识别事件 Webhook 推送 | 事件 → adapter 解析 → Webhook → parking-system `DeviceWebhookController`；EventOutbox 重试 | ✅ |
| 设备注册 / 心跳 / 状态管理 | DeviceRegistry / DeviceProductRegistry / DeviceRelationRegistry | ✅ |
| 主动抓拍（v0.7） | `POST /api/v1/devices/{sn}/capture`；芊熠 snapshot 优先、tarkphoto 回退 | ✅ |
| 抓拍图片存储与清理 | 本地落盘 + URL 访问映射，每日 03:20 清理（默认保留 30 天） | ✅ |
| 显示屏（OlmM1d）/ 语音 | 下行命令支持 | ✅ |

> 代码索引：`backend/device-access.md`；协议文档：`docs/协议/{臻识,芊熠}/`

## 四、岗亭作业

| 功能 | 入口 | 状态 |
|---|---|---|
| 识别事件全自动处理（识别→类型判定→余位→计费→开闸→日志→WS推送） | `POST /api/v1/booth/recognition/handle` | ✅ |
| 人工开闸 / 关闸 / 常开锁定 / 取消常开 / 常关锁定 / 取消常关 / 批量开闸（统一走 `DeviceService.resolveGateDevice(laneId)` 解析控闸设备） | `/api/v1/booth/recognition/manual-*` | ✅ |
| 手动抓拍（严格按入口相机解析；臻识/芊熠双品牌，臻识走 MQTT snapshot 两段式 + OSS 下载落盘） | `POST /api/v1/booth/recognition/manual-capture` | ✅ |
| 车道控闸能力查询（Device.capabilities 空则 fallback 到 DeviceModel.capabilities，前端按钮按能力渲染） | `GET /api/v1/booth/recognition/gate-capabilities` | ✅ |
| 人工补录入场（GAP-08） | `POST /api/v1/booth/manual-entry` | 🚧 **缺 `@RequirePermission`**，登录即可调 |
| 车牌识别纠错 | `/api/booth/recognition`（BoothRecognitionController） | ✅ |
| 无牌车/临时车牌：建议、人工入场、出场匹配 | `/api/booth/temp-plate` | ✅ |
| 岗亭查费 / 费用预览 | `/api/booth/parking/fee` | ✅ |
| 费用减免（权限 `fee:reduce` + 日志） | `POST /api/v1/booth/charge/fee-reduction` | ✅ |
| 交接班（开班/交班/当班查询/现金订单汇总） | `/api/v1/shift-records` | ✅ |
| 岗亭监控（快照/设备刷新/异常确认） | `/api/booth/monitor` + monitor_alert | ✅ |
| 远程开闸 | `/api/v1/admin/remote-gate`、`/api/v1/internal/gate` | ✅ |
| 岗亭断网前端兜底 | 前端 `offline-queue.ts`：离线请求存 localStorage、恢复后重放 | 🚧 仅请求重放；**本地开闸 + 记录补传未实现** |
| 岗亭端前端页面 | `frontend/`：登录、监控大屏（车道卡片/抓拍图实时上卡）、收费面板、人工放行/车牌纠正/临时车牌弹窗、运营页（通行记录等） | ✅ |

> 代码索引：`modules-booth.md`、`frontend/frontend.md`

## 五、车辆与固定车管理

| 功能 | 入口 | 主要数据表 | 状态 |
|---|---|---|---|
| 车辆主档 CRUD / 批量删除 / 分页 | `/api/v1/vehicles` | sys_vehicle | ✅ |
| 一位多车绑定 / 解绑 | `/api/v1/vehicles/{id}/multi-plates` | sys_vehicle_multi_plate | ✅ |
| 车辆资料审核 | `/api/v1/vehicle-audits` | vehicle_audit | ✅ |
| 黑白名单 CRUD / 导入 / 触发类型 | `/api/v1/admin/vehicle-list` | vehicle_list | ✅ |
| 车辆类型判定引擎（黑名单→超级车牌→VIP→月租→储值→免费→临时） | VehicleTypeDecisionService（被识别链路调用） | — | ✅ |
| 储值车钱包：充值/退款/调账/流水 | `/api/v1/vehicle-wallets` | sys_vehicle_wallet、sys_vehicle_wallet_log | ✅ |
| 月卡/固定车续费闭环（发起→订单→支付回调→回写有效期，幂等） | `/api/v1/vehicles/{id}/renew*` | 复用订单表 + vehicle_renewal_log | ✅ |
| 内部白名单同步 | `/api/v1/internal/whitelist` | — | ✅ |

> 代码索引：`modules-vehicle.md`

## 六、计费

| 功能 | 入口 | 主要数据表 | 状态 |
|---|---|---|---|
| **旧**收费规则 CRUD / 版本切换 / 试算 | `/api/admin/billing-rules`（com.jushan.system） | billing_rule、billing_rule_recalc_log | ✅ 线上在用 |
| **新**计费规则引擎：按时/按次/阶梯/分时段 4 模式，免费时长、首时段、每日封顶、跨天（自然日/连续24h）、优先级（区域>车场>平台默认） | `/api/v1/fee-rules`、`/api/v1/fee/calculate` | fee_rule、fee_rule_segment | ⚠️ **冻结**（标注「二期计费体系候选」） |
| 出场自动计费（收敛入口 calculateFeeCents，整数分） | FeeCalculationService（被出场/识别链路调用） | — | ✅ |

> ⚠️ 两套计费并存是已知决策点，见 [`待实现需求.md`](待实现需求.md) 风险节。代码索引：`modules-parking.md`

## 七、停车会话与记录

| 功能 | 入口 | 主要数据表 | 状态 |
|---|---|---|---|
| 入场 / 出场 / 在场查询 / 在场统计（出场识别幂等判重） | `/api/v1/parking-sessions` | parking_session | ✅ |
| 停车记录分页 / 导出（含入场抓拍图） | `/api/v1/admin/parking-records` | parking_record | ✅ |
| 岗亭在场车辆（含计费）/ 历史通行记录 | `/api/v1/booth/vehicles/present|history` | — | ✅ |
| 重复入场策略 | （迁移 V20260713001） | duplicate_entry_policy | ✅ |
| 数据归档查询 | `/api/v1/archive` | archive 相关表 | ✅ |

## 八、订单与支付

| 功能 | 入口 | 状态 |
|---|---|---|
| 停车订单：支付 / 取消 / 查询 | `/api/v1/orders` | ✅ |
| 订单管理：分页/详情/关闭/退款/状态日志/导出 | `/api/v1/admin/orders` | ✅ |
| P云对接：支付回调（幂等）、前端接口（计费/支付结果/车辆信息/续费通知）、VIP 同步 | `/api/v1/pay`、`/api/v1/pyun` | ✅ 过渡方案 |
| **模拟支付**（配置/记录/手动标记支付） | `/api/v1/mock-payment` | 🧪 **占位实现**，供联调演示 |
| 缴费后免费出场窗口（pay window） | （迁移 V20260823001） | 🚧 表结构已有，规格化待二期确认 |
| 欠费处理（欠费订单 ids、放行/提醒/补缴消息） | arrears_order_ids + MiniMessage | 🚧 |

> 代码索引：`system-controller.md` 三/五/六节、`modules-miniapp.md`

## 九、月卡与固定车位

| 功能 | 入口 | 主要数据表 | 状态 |
|---|---|---|---|
| 月卡 CRUD / 到期列表 / 续费 / 取消 | `/api/v1/monthly-passes` | monthly_pass | ✅ |
| 月卡审批 | `/api/v1/admin/monthly-pass-audit` | — | ✅ |
| 固定车位 CRUD / 到期列表 / 续费 / 审核 | `/api/v1/fixed-spaces`、`/api/v1/admin/fixed-space-audit` | fixed_space_binding | ✅ |
| 小程序月卡申请 / 支付 / 续费 | `/api/v1/mini/monthly-passes` | — | ✅ 后端接口（前端未启动） |
| 小程序固定车位申请 / 支付 / 续费 / 可用查询 | `/api/v1/mini/fixed-spaces` | — | ✅ 后端接口（前端未启动） |
| 月卡/固定车位价格参数 | （迁移 monthly_pass_price_param / fixed_space_price_param） | parking_lot_param | ✅ |

> 未做：到期自动转临时计费、宽限期、到期前 N 天提醒 —— 见 [`待实现需求.md`](待实现需求.md) P1。

## 十、小程序端（后端接口先行）

| 功能 | 入口 | 状态 |
|---|---|---|
| 微信登录 / 退出 / 用户信息 / 手机绑定 | `/wx`、`/api/v1/mini` | ✅ |
| 车牌绑定 / 解绑 / 默认车牌（绑定数上限） | `/api/v1/mini/plates` | ✅ |
| 停车记录（tab 筛选/详情/按车牌）/ 当前在场 | `/api/v1/mini/parking-records*` | ✅ |
| 车主查费 / 费用预览 | `/wx/parking/fee` | ✅ |
| 支付预下单 / 结果通知 | `/api/v1/mini/pay/prepare|notify` | ✅ |
| 代理支付（代缴，双方消息通知） | `/api/v1/mini/pay/proxy-*` + proxy_pay_record | ✅ |
| 消息通知（列表/已读/未读数） | `/api/v1/mini/messages` | ✅ |
| 访客预约提交 / 审核 / 取消 | `/api/v1/visitor-applies` | ✅ |
| 余位查询 / 附近车场 / 车场列表 | `/api/v1/mini/parking-lot/*`、`/api/v1/mini/parking-lots` | ✅ |
| **小程序前端** | `miniapp/` 目录 | ◻️ **未启动（目录为空）** |

> 代码索引：`modules-miniapp.md`、`system-controller.md` 四节

## 十一、经营数据

| 功能 | 入口 | 状态 |
|---|---|---|
| 营收 / 车流报表 + 导出 | `/api/v1/admin/reports` | ✅ 未按二期规格验收（现金/线上分列、临停/月卡占比待核实） |
| 运营仪表盘 | `/api/v1/admin/dashboard` | ✅ 基础版 |
| 运营概览统计（按 period/日期范围） | `/api/v1/admin/analytics/overview` | ✅ |
| 余位计算 / 余位展示 | `/api/v1/parking-space-policies/lot/{id}/remain` + remaining_spaces | ✅ |

## 十二、其他

| 功能 | 入口 | 状态 |
|---|---|---|
| 异常记录列表 / 处理 | `/api/v1/admin/exception-records` | ✅ |
| 人工开闸记录 | `/api/v1/admin/manual-gate-records` | ✅ |
| 模拟识别事件（内部联调） | `/api/v1/internal/mock` | 🧪 联调用 |
| 超时黑名单任务 | `com.jushan.platform.task.OverstayBlacklistService` | ✅ |

---

## 十三、占位 / 半成品 / 冻结汇总（二期重点消化对象）

| 项 | 状态 | 位置 | 处理方向 |
|---|---|---|---|
| 模拟支付 | 🧪 占位 | MockPaymentController + mock payment 表 | 二期保留开关用于联调，真实微信支付上线后降级 |
| P云对接 | 🧪 过渡 | PyunNotify/PyunPpFront/PyunVip | 二期微信支付服务商模式上线后评估去留 |
| 新计费引擎（FeeRule/FeeCalculation/ParkingZone） | ⚠️ 冻结 | modules/parking | 二期 P0 解冻定版，替代旧 BillingRule |
| 人工补录入场缺权限 | 🚧 | BoothManualEntryController（GAP-08） | 补 `@RequirePermission` |
| 岗亭断网本地开闸 + 补传 | 🚧 仅前端离线队列 | frontend offline-queue | 二期 P0 异常场景规格化 |
| 小程序前端 | ◻️ | miniapp/（空） | 二期 P2 启动 |
| 对账 | ◻️ | 无 | 二期 P0 新建 |
