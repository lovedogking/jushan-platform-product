# 飓山智慧停车 — 服务器配置手册

> **服务器 IP**：`120.26.3.4`
> **操作系统**：Ubuntu 22.04（Linux 5.15.0-181-generic）
> **最后更新**：2026-07-25（v1.6：Webhook URL 统一由 .env.production 管理；芊熠 Q3 图片通过 MQTT base64 直传，不再依赖 HTTP 独立上传）

---

## 一、架构总览

```
┌────────────────────────────────────────────────────┐
│                    阿里云 ECS                        │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐ │
│  │ 相机(臻识)│  │ 相机(芊熠)│  │ 岗亭浏览器          │ │
│  └────┬─────┘  └────┬─────┘  └────────┬──────────┘ │
│       │ MQTT:1883   │ MQTT:1883      │ HTTP:80     │
│       ▼             ▼                 ▼             │
│  ┌──────────────────────────────────────────────┐  │
│  │              Nginx (:80)                      │  │
│  │  /          → 前端 SPA                        │  │
│  │  /api/*     → backend:8080                   │  │
│  │  /images/*  → 静态图片（磁盘直读）              │  │
│  │  /ws/*      → WebSocket → backend:8080       │  │
│  └──────────────────────────────────────────────┘  │
│       │                                             │
│       ▼                                             │
│  ┌──────────────────────────────────────────────┐  │
│  │        parking-boot (Docker) :8080            │  │
│  │        Spring Boot 3.5 · Java 21              │  │
│  └──────┬───────┬────────┬────────┬─────────────┘  │
│         │       │        │        │                  │
│         ▼       ▼        ▼        ▼                  │
│  ┌──────┐ ┌──────┐ ┌────────┐ ┌──────┐             │
│  │MySQL │ │Redis │ │RabbitMQ│ │EMQX  │             │
│  │:3306 │ │:6379 │ │ :5672  │ │:1883 │             │
│  └──────┘ └──────┘ └────────┘ └──────┘             │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │     device-access (宿主机进程) :8082           │  │
│  │     独立 jar 运行，非 Docker                    │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │     vsftpd (FTP) :21 + :30000-30010          │  │
│  │     相机 FTP 上传图片                           │  │
│  └──────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

## 二、Docker 容器服务

所有容器通过 `/opt/jushan/docker-compose.prod.yml` + `/opt/jushan/.env.production` 管理。

```bash
cd /opt/jushan
docker compose -f docker-compose.prod.yml --env-file .env.production up -d
```

| 容器名 | 镜像 | 端口映射 | 用途 |
|--------|------|----------|------|
| `jushan-mysql` | `mysql:8.4` | `127.0.0.1:3306→3306` | 主数据库 |
| `jushan-redis` | `redis:7-alpine` | 内网 `6379` | 缓存/会话/WebSocket |
| `jushan-rabbitmq` | `rabbitmq:3.12-management` | 内网 `5672` | 消息队列 |
| `jushan-emqx` | `emqx/emqx:5.8.5` | `0.0.0.0:1883`, `127.0.0.1:18083` | MQTT Broker |
| `jushan-backend` | `jushan-platform:latest` | 内网 `8080` | Spring Boot 后端（Webhook 接收地址 http://127.0.0.1/api/v1/device-webhook/events 经 nginx） |
| `jushan-nginx` | `nginx:alpine` | `0.0.0.0:80` | 反向代理 + 前端静态资源 |

---

## 三、数据库

### MySQL

| 配置项 | 值 |
|--------|-----|
| 地址 | `127.0.0.1:3306` |
| 数据库名 | `jushan_platform` |
| 用户名 | `jushan` |
| 密码 | `8Lkohb4oMzkmfaro554XaZbW6O4=` |
| Root 密码 | `fcsVObYdbouJQu/P6yPydVw/yoKWZWlq` |

**device-access 独立库**（同一 MySQL 实例）：

| 配置项 | 值 |
|--------|-----|
| 数据库名 | `device_access` |
| 用户名 | `jushan` |
| 密码 | 同上 |

连接命令：
```bash
docker exec -it jushan-mysql mysql -u jushan -p
```

### Redis

| 配置项 | 值 |
|--------|-----|
| 地址 | `redis:6379`（容器内）/ `127.0.0.1:6379`（宿主机） |
| 密码 | `9GuTpXvvTAzY286tCbJOKWq7kg0=` |
| 最大内存 | 512MB |
| 淘汰策略 | `allkeys-lru` |

---

## 四、MQTT（EMQX）

| 配置项 | 值 |
|--------|-----|
| Broker 地址 | `tcp://120.26.3.4:1883` |
| Dashboard | `http://127.0.0.1:18083` |
| 管理员用户名 | `admin` |
| 管理员密码 | `9GuTpXvvTAzY286tCbJOKWq7kg0=` |
| 匿名访问 | 已开启（相机无需用户名密码） |

---

## 五、Web 服务 & 认证

### 后端 (parking-boot)

| 配置项 | 值 |
|--------|-----|
| 内部地址 | `http://backend:8080`（容器内）/ 经 nginx 80 端口代理 |
| JWT 密钥 | `mxpKz5I6Rk4t9NKUAx0zlQ5d9m6rt+FmVKpQs85vJ+8JezGBh1AZeQBy+UuORBP4` |
| JWT 过期 | 24 小时 |
| JWT Refresh 过期 | 7 天 |
| 管理员初始密码 | `x6rHjL2NAPa7STwVGQIC2A==` |

### Device Access API Key

| 配置项 | 值 |
|--------|-----|
| 地址 | `http://host.docker.internal:8082`（容器内）/ `http://127.0.0.1:8082`（宿主机） |
| API Key | `Jem7uHVc1ahXBof82paWSrW9f5U0s/D7` |
| Whitelist Sync API Key | `w4f5umozc6DpdkAyhiAyIROpuT7HpHEe` |

### Webhook 事件推送

> **重要**：Webhook URL **必须走 nginx 80 端口**，不能直连 `127.0.0.1:8080`（Docker 内网端口，宿主机不可达）。
> 该 URL 统一由 `/opt/jushan/.env.production` 中的 `WEBHOOK_URL` 变量管理，`restart-device-access.sh` 启动时自动加载。

| 配置项 | 值 |
|--------|-----|
| Webhook URL | `http://127.0.0.1/api/v1/device-webhook/events`（经 nginx） |
| 签名密钥 | `0z7fC5HzUKeLb35FANpo8ysvGNDI1961SwN07RTugw4=` |
| 签名算法 | HMAC-SHA256(timestamp + nonce + body) |
| IP 白名单 | `127.0.0.1,172.` |

---

## 六、RabbitMQ

| 配置项 | 值 |
|--------|-----|
| 地址 | `rabbitmq:5672`（容器内） |
| 用户名 | `admin` |
| 密码 | `/5aV0CbR6q6Jj5OL0WSPYoi3N1A=` |
| Virtual Host | `/` |
| 管理界面 | 未对外暴露（仅内网 `15672`） |

---

## 七、FTP（相机图片上传）

| 配置项 | 值 |
|--------|-----|
| 服务 | vsftpd |
| 地址 | `120.26.3.4:21` |
| 用户名 | `camera` |
| 密码 | `l3M38o2kDtJhXefi` |
| 上传根目录 | `/opt/jushan/data/device-images/` |
| 被动模式端口 | `30000-30010` |

相机配置 FTP 上传后，图片 URL 格式为：
```
http://120.26.3.4/images/{yyyyMMdd}/{sn}/{时间戳}.jpg
http://120.26.3.4/images/{yyyyMMdd}/{sn}/{时间戳}_plate.jpg  （车牌特写）
```

---

## 八、阿里云安全组入方向规则

| 端口 | 协议 | 用途 | 授权来源 |
|------|------|------|----------|
| 80 | TCP | Nginx（前端 + API） | `0.0.0.0/0` |
| 1883 | TCP | MQTT（相机连接） | 相机公网 IP 或 `0.0.0.0/0` |
| 21 | TCP | FTP 控制 | 同上 |
| 30000-30010 | TCP | FTP 被动模式数据 | 同上 |
| 22 | TCP | SSH 运维 | 办公 IP |

---

## 九、目录结构

```
/opt/jushan/
├── docker-compose.prod.yml    # Docker Compose 配置
├── .env.production            # 环境变量（密码等机密信息）
├── Dockerfile                 # 后端镜像构建文件
├── docker/
│   ├── mysql/init/            # MySQL 初始化脚本
│   └── nginx/
│       ├── nginx.conf
│       └── conf.d/production.conf
├── parking-boot/target/       # 后端 JAR 包
├── device-access/
│   └── device-access-starter/target/  # device-access JAR 包
├── frontend/dist/             # 前端构建产物（nginx 直接挂载）
├── data/device-images/        # 相机图片存储（FTP 上传 + nginx 静态服务）
├── logs/
│   └── device-access.log      # device-access 日志
├── start-da.sh                # device-access 启动脚本
└── restart-device-access.sh   # device-access 重启脚本
```

---

## 十、常用运维命令

### 查看所有容器状态
```bash
cd /opt/jushan && docker compose -f docker-compose.prod.yml ps
```

### 重启后端
```bash
cd /opt/jushan
# 1. 构建新 JAR：mvn clean package -pl parking-boot -am -Dmaven.test.skip=true
# 2. 上传 parking-boot/target/parking-boot-*.jar 到 /opt/jushan/parking-boot/target/
# 3. 重建并重启
docker compose -f docker-compose.prod.yml --env-file .env.production build backend
docker compose -f docker-compose.prod.yml --env-file .env.production up -d backend
```

### 重启 device-access

> 推荐使用 `restart-device-access.sh`，它会自动从 `.env.production` 加载 `WEBHOOK_URL` 等环境变量。

```bash
/opt/jushan/restart-device-access.sh
```

### 查看后端日志
```bash
docker logs -f --tail 100 jushan-backend
```

### 查看 device-access 日志
```bash
tail -f /opt/jushan/logs/device-access.log
```

### 连接 MySQL
```bash
docker exec -it jushan-mysql mysql -u jushan -p
```

### 查看 EMQX 客户端
```bash
docker exec jushan-emqx emqx ctl clients list
```

### 部署前端
```bash
# 1. 本地构建：cd frontend && npx vite build
# 2. 上传：rsync -avz --delete dist/ root@120.26.3.4:/opt/jushan/frontend/dist/
```

### 芊熠 Q3 图片链路（v1.6）

芊熠 Q3 相机（固件 20.0.471）默认 `alonepush=0`，图片通过 MQTT `result` 消息的 `full_pic`/`plate_pic` 字段以 base64 编码直传，由 device-access `PlateRecognizedEventDispatcher` 解码落盘。

若后续启用 `alonepush=1`，相机将走 HTTP `/images/upload` 独立上传路径。

### 臻识 C5 图片链路

臻识 C5 通过 FTP 主动上传图片到 `/opt/jushan/data/device-images/`，或通过 MQTT `snapshot` 命令主动抓拍。事件携带的 OSS 签名 URL 由 `RemoteImageDownloader` 在事件到达时立即转存本地。

### 重启 FTP
```bash
systemctl restart vsftpd
```

---

## 十一、启动顺序

1. Docker 基础服务（自动启动）：
   ```bash
   cd /opt/jushan
   docker compose -f docker-compose.prod.yml --env-file .env.production up -d mysql redis rabbitmq emqx
   ```

2. 后端（依赖 MySQL/Redis/RabbitMQ 健康检查通过后自动启动）：
   ```bash
   docker compose -f docker-compose.prod.yml --env-file .env.production up -d backend nginx
   ```

3. device-access（宿主机进程，需后端 + EMQX 就绪后）：
   ```bash
   nohup /opt/jushan/start-da.sh > /tmp/start-da.out 2>&1 &
   ```
