# 开发任务包 阶段 1~6 审查报告

> 基准代码：develop 分支 @ 2026-07-18
> 审查范围：阶段 1（地基）→ 阶段 6（运营端），共 18 个任务包
> 配套文档：《需求规格说明书 V1.3》《差距分析报告 V1.1》《开发任务提示词包 V1.2》
> 编译状态：`mvn clean compile -pl parking-system -am` → **BUILD SUCCESS**

---

## 一、总体结论

| 阶段 | 任务包数 | 完成 | 部分完成 | 未完成 | 完成率 |
|:---|:---:|:---:|:---:|:---:|:---:|
| 阶段 1：地基 | 3 | 3 | 0 | 0 | 100% |
| 阶段 2：收费闭环 | 3 | 3 | 0 | 0 | 100% |
| 阶段 3：车辆体系 | 5 | 5 | 0 | 0 | 100% |
| 阶段 4：岗亭端 | 3 | 3 | 0 | 0 | 100% |
| 阶段 5：小程序端 | 3 | 3 | 0 | 0 | 100% |
| 阶段 6：运营端 | 2 | 2 | 0 | 0 | 100% |
| **合计** | **18** | **18** | **0** | **0** | **100%** |

> 注：阶段 4 任务包 4-1 中的车牌校正功能（BOOTH-005）原审查时为缺失项，已于本次审查周期内补充实现（提交 b9b41cac~eb578f8d）。

---

## 二、逐任务包审查详情

---

### 阶段 1：地基

#### 任务包 1-1：车场级参数体系 ✅

| 验收项 | 证据 |
|:---|:---|
| sys_config 扩展 parking_lot_id + param_level | Flyway `V20260820001__task_0101_parking_lot_param.sql`：ALTER TABLE 添加两列 + 调整唯一键 |
| 7 项车场参数定义 | `ParamKeys.java:51-69` — mock_payment.timeout_minutes、exit.unpaid_strategy、arrears.reexit_strategy、recognition.fail_strategy、monthly_pass.expiry_reminder_days、monthly_pass.count_in_available_space、pay.exit_window_minutes |
| ParamResolver 优先级解析 | `ParamResolver.java:69` — 车场级 → 全局 → 默认值三级回退，走 Redis 缓存 |
| admin-web 车场参数配置 UI | `ParkingLotParamDrawer.vue` — 继承标识/车场专属切换/重置为继承 |
| 权限控制 | `@RequirePermission` + `@BusinessLog` 写操作记录 |

**判定**：✅ 完成

---

#### 任务包 1-2：订单状态机扩展 + 模拟退款 ✅

| 验收项 | 证据 |
|:---|:---|
| PRE_ORDER / ARREARS 状态常量 | `ParkingOrder.java:147,155` |
| order_status_log 状态流转日志表 | Flyway `V20260821001__...sql:35-52` |
| EntryService 入场生成预订单 | `EntryService.java:212-252` — `createPreOrderIfChargeable()`，月卡/固定车位/白名单不建订单 |
| ExitService 预订单→计费→待支付 | `ExitService.java:197-309` — PRE_ORDER 路径零费→完成/有费→待支付 |
| 退款闭环 | `OrderAdminController.java:264-291` — POST /{id}/refund，仅 PAID 可退 |
| admin-web 退款操作 | `order/index.vue:79-81` — PAID 状态显示退款按钮 + 弹窗填原因 |
| 6 条流转路径 | 预订单→待支付→已支付→已完成、待支付→已取消、待支付→欠费中、已支付→已退款、预订单→已完成（免费放行），均有状态机守卫 |

**判定**：✅ 完成

---

#### 任务包 1-3：计费规则收敛与生效方式对齐 ✅

| 验收项 | 证据 |
|:---|:---|
| billing_rule 新增 effect_type | Flyway `V20260822001__billing_effect_type.sql`：IMMEDIATE / NEW_ENTRY_ONLY / SCHEDULED |
| billing_rule 新增 effect_time | 同上，SCHEDULED 时必填 |
| parking_record 规则快照 | `V20260822001...:24-25` — rule_snapshot JSON 列；`ParkingRecord.java:71` |
| BillingEngine 快照计费 | `BillingEngine.java:208` — `calculateFeeFromSnapshot()` |
| 入场时快照保存 | `EntryService.java:387` — NEW_ENTRY_ONLY 下写快照 |
| admin-web 生效方式选择器 | `BillingRuleFormModal.vue:28-42` — 下拉选择 + 定时时间选择器 |
| 三种生效方式常量 | `BillingRule.java:75-77` |

**判定**：✅ 完成

---

### 阶段 2：收费闭环

#### 任务包 2-1：支付出场窗口期 ✅

| 验收项 | 证据 |
|:---|:---|
| parking_order.pay_window_deadline | Flyway `V20260823001__task_0201_pay_window.sql:5` |
| parking_order.pay_scene (AT_EXIT / ADVANCE) | 同上:6；`ParkingOrder.java:110-111,165-168` |
| MiniPayController 场景判定 | `MiniPayController.java:132-141` — 小程序端始终 ADVANCE |
| ExitService 出口场景 | `ExitService.java:280,289,300,622` — 出口识别始终 AT_EXIT |
| 窗口期内自动开闸 | `ExitService.java:200-225` — `now ≤ deadline` → 自动开闸放行 |
| 超期重算 | `ExitService.java:209-214` — `createRecalcOrder()` 关联原订单 |
| 提前缴费窗口期设置 | `MockPaymentService.java:260-266` — 读 `pay.exit_window_minutes` 参数 |

**判定**：✅ 完成

---

#### 任务包 2-2：超时关单重新计费 ✅

| 验收项 | 证据 |
|:---|:---|
| timeoutClose 读 ParamResolver | `MockPaymentService.java:99,350-387` — 读 `mock_payment.timeout_minutes` 车场参数 |
| 建单收敛三路径 | `ExitService.java:227-308` — PENDING_PAY 复用/CANCELLED 重算/PRE_ORDER 计费 |
| 幂等防重 | 分布式锁 `exit:order:lock:{recordId}` + `findReusableOrderForRecord` + `idempotencyKey` |
| billing_rule_recalc_log 审计 | `V20260824001__extend_billing_rule_recalc_log.sql` — originalOrderId/originalAmountCents/newAmountCents/triggerReason |
| admin-web 重算关联展示 | `order/index.vue:1058-1065` — "重算关联"可点击链接跳转原订单（本次补充完成） |

**判定**：✅ 完成（原 admin-web 关联展示已通过 Fix 2 补充）

---

#### 任务包 2-3：欠费全链路 ✅

| 验收项 | 证据 |
|:---|:---|
| exit.unpaid_strategy 两种策略 | `ExitService.java:396-423` — BLOCK 拦截 / ALLOW_ARREARS 欠费放行 |
| arrears.reexit_strategy 两种策略 | `ExitService.java:594-694` — MUST_PAY 合并计费 / REMIND_ONLY 提醒放行 |
| 合并计费 arrearsOrderIds | `ParkingOrder.java:119-120` — JSON 数组 `[101,102]` |
| 小程序欠费展示 | `MiniParkingRecordVO.java:44-45` — ARREARS 状态；`records.wxml:44` — 红色 "欠费" badge |
| 欠费补缴 | `MockPaymentService.java:171-190` — ARREARS→COMPLETED；`503-537` — `processArrearsChainPayment` 逐条补缴 |
| 欠费站内消息 | `MiniMessageService.java:32-49` — 放行/提醒/补缴成功三类通知 |
| 4 组策略组合 | BLOCK×MUST_PAY、BLOCK×REMIND_ONLY、ALLOW_ARREARS×MUST_PAY、ALLOW_ARREARS×REMIND_ONLY 均有代码路径 |

**判定**：✅ 完成

---

### 阶段 3：车辆体系

#### 任务包 3-1：月卡独立实体重建 ✅

| 验收项 | 证据 |
|:---|:---|
| monthly_pass 表 | Flyway `V20260826001__create_monthly_pass.sql` — 完整表结构 + 索引 |
| MonthlyPass 实体字段 | `MonthlyPass.java` — plateNumber/vehicleType/parkingLotId/validStartDate/validEndDate/amountCents/paidAmountCents/payMethod/passStatus/reviewStatus/applicantId/source |
| 审核流 | `MonthlyPassAuditController` — approve/reject；AUTO/MANUAL 审核模式读车场参数 |
| 续期/注销 | `MonthlyPassService.java` — renew/cancel |
| VehicleTypeDecisionService 读月卡 | `VehicleTypeDecisionServiceImpl.java:151-166` — 最高优先级查 monthly_pass |
| MonthlyPassExpiryJob | 每日 2:00 定时扫描到期置已过期 |
| 存量迁移 | INSERT INTO monthly_pass SELECT FROM sys_vehicle WHERE vehicle_type='MONTHLY' + 旧数据重写为 FREE |

**判定**：✅ 完成

---

#### 任务包 3-2：固定车位对齐 V1.1 ✅

| 验收项 | 证据 |
|:---|:---|
| 审核字段补齐 | `V20260827001__add_fixed_space_columns.sql` — pay_method/paid_amount_cents/review_status/source/applicant_id |
| FixedSpaceService 审核 | `FixedSpaceService.java:654,695` — approve/reject |
| FixedSpaceAuditController | 审核通过/拒绝接口 |
| 车位号唯一校验 | 权限检查 + 保存拦截 |
| FixedSpaceExpiryScheduler | 每日 00:05 扫描到期 |
| 区域配额校验 | ParkingSpacePolicy.fixedSpaces 配额检查 |

**判定**：✅ 完成

---

#### 任务包 3-3：黑白名单独立实体 ✅

| 验收项 | 证据 |
|:---|:---|
| vehicle_list 表 | Flyway `V20260828001__create_vehicle_list.sql` |
| VehicleList 实体 | plateNumber/listType/parkingLotId/startDate/endDate/triggerType/status/remark |
| 互斥校验 | `VehicleListService.java:69-76` — 同车场同车牌互斥检查 |
| 三种触发模式 | VehicleTypeDecisionService — DENY_ENTRY/ALLOW_WITH_ALERT/BY_TYPE |
| EntryService 拦截 | `EntryService.java:161-182` — denyEntry 抛异常 / alert 创建告警 |
| ExitService 白名单/黑名单 | 白名单返回 allow=true chargeable=false |
| 存量迁移 | AccessPolicy + sys_vehicle 数据订正到 vehicle_list |
| 自动拉黑开关 | `OverstayBlacklistScheduler` — `@ConditionalOnProperty` matchIfMissing=false，默认关闭 |

**判定**：✅ 完成

---

#### 任务包 3-4：无牌车临时车牌 ✅

| 验收项 | 证据 |
|:---|:---|
| temp_plate_flag 字段 | `V20260717005__add_temp_plate_flag.sql` — parking_record/parking_order/recognition_event_log 三表 |
| TempPlateService | manualEntry（生成记录+预订单+开闸）/ handleExitMatch（计费+现金收费+开闸+出场记录） |
| TempPlateNumberGenerator | Redis INCR 生成 `临{yyMMdd}{seq02}`，fallback 时间戳 |
| BoothTempPlateController | GET suggest / POST entry / POST exit-match |
| RecognitionEventConsumer 失败分发 | MANUAL 推送岗亭告警 / AUTO_RELEASE 自动生成临时车牌入场 |
| 岗亭 UI | TempPlateAlertModal — WebSocket 告警弹窗 + 处理面板 |

**判定**：✅ 完成

---

#### 任务包 3-5：双向通道与主备相机逻辑 ✅

| 验收项 | 证据 |
|:---|:---|
| admin-web 双向通道 UI | `ParkingLaneManage.vue:126-147` — 入场/出场双相机选择器 + 完整性校验提示 |
| CameraFailoverService 主备切换 | onDeviceOffline 切备相机 + 60s 防抖 / onDeviceOnline 自动回切 |
| cameraSource 字段全链路 | RecognitionEventLog / ParkingRecord / ExitRecord / RecognitionEventPayload 均有 |
| 识别事件方向判定 | Consumer 优先按相机 recognition_direction 判定，臻识 direction=4 兜底 |
| 岗亭主备状态标识 | `monitor/index.vue:631-632` — "主相机离线"告警 + 当前活跃相机来源 |

**判定**：✅ 完成

---

### 阶段 4：岗亭端

#### 任务包 4-1：通行作业与收费处理对齐 ✅

| 验收项 | 证据 |
|:---|:---|
| 手动开闸计费开关 | `ManualReleaseModal.vue:11-47` — 是否计费 a-switch + 金额输入 |
| 费用减免接口 | `BoothFeeReductionController` — POST /api/v1/booth/charge/fee-reduction，@RequirePermission("fee:reduce") |
| 扫码支付隐藏 | `ChargePanel.vue:72-78` — 微信/支付宝 radio 被注释 |
| 大额二次确认 | `ChargePanel.vue:489` — ≥50000 分（500元）弹窗确认 |
| 车牌校正 | `PlateCorrectionModal.vue` + `BoothRecognitionController` — 校正弹窗 + POST /api/booth/recognition/{logId}/correct（本次补充完成） |

**判定**：✅ 完成（车牌校正已在审查周期内补充）

---

#### 任务包 4-2：交接班钱箱核对 ✅

| 验收项 | 证据 |
|:---|:---|
| shift_record 扩展字段 | fee_amount（应收）/ cash_amount（实收）/ adjust_reason（修正原因）/ arrears_count / handover_order_count |
| close 统计方法 | `ShiftRecordServiceImpl` — 按车场统计应收/实收 |
| 修正留痕 | ShiftCloseCmd — confirmedCashAmount + adjustReason |
| 欠费订单移交 | `ShiftHandover.vue:133-148` — "以下欠费订单将交接至下一班次" |
| 岗亭交班页面 | `ShiftHandover.vue` — 当前班次信息 + 统计 + 确认/修正 + 交班按钮 + 历史列表 |

**判定**：✅ 完成

---

#### 任务包 4-3：岗亭布局与查询页 ✅

| 验收项 | 证据 |
|:---|:---|
| 三段式布局 | `ParkingLotSidebar.vue` — 左侧车场列表 + 右侧视频占位/操作面板/通行记录 Tab |
| sessionStorage 登录态 | `router/index.ts:45` — `sessionStorage.getItem(TOKEN_KEY)`，全 booth-web 无 localStorage 引用 |
| 在场车辆查询 | `VehicleQuery.vue` — 在场车辆列表 + 历史记录，双排序：入场时间 / 停车时长 |
| 右下角识别失败通知 | `notification.warning({ placement: 'bottomRight', duration: 0 })` — 非阻塞 + 未处理计数角标 |
| 远程开闸弹窗自动消失 | 读全局参数 remote_gate.popup_timeout_seconds |

**判定**：✅ 完成

---

### 阶段 5：小程序端

#### 任务包 5-1：微信登录链路与手机号绑定 ✅

| 验收项 | 证据 |
|:---|:---|
| wx.login → doLogin | `app.js:11,47-67` — wxLoginPromise → code → /api/v1/mini/login → token |
| 手机号快速验证 | `bind-phone.wxml:8` — `<button open-type="getPhoneNumber">` |
| MiniAuthController | `POST /api/v1/mini/login` + `POST /api/v1/mini/phone` |
| WeChatApiClient | code2session + getPhoneNumber 调微信接口 |
| JWT audience 校验 | `JwtAuthenticationFilter` — /api/v1/mini/** 专用 audience |
| phoneBound 守卫 | pay/proxy-pay/monthly-pass/fixed-space/plate/profile 共 8 个页面 |
| 生产环境 mock 保护 | `WxMiniappConfig` — prod profile 下 mock-login 为 false |
| app.json 补充 | lot-space/messages/proxy-pay/bind-phone 的 .json 配置 |

**判定**：✅ 完成

---

#### 任务包 5-2：月卡/固定车位小程序链路 ✅

| 验收项 | 证据 |
|:---|:---|
| 月卡小程序页 | pages/monthly-pass/ — apply（申请）/ monthly-pass（列表）/ renew（续费） |
| 固定车位小程序页 | pages/fixed-space/ — apply（申请 + 车位选择器）/ fixed-space（列表）/ renew（续费） |
| MiniMonthlyPassController | `POST /api/v1/mini/monthly-passes/apply` + list/detail/pay/renew |
| MiniFixedSpaceController | 同上对称接口 |
| 审核模式 | 读车场参数 AUTO/MANUAL，审核通过后小程序内模拟支付 |
| 续费有效期顺延 | 从原截止日顺延一个自然月周期 |
| 我的页面入口替换 | profile 页 toast 占位 → 真实月卡/固定车位入口 |

**判定**：✅ 完成

---

#### 任务包 5-3：记录结构/订阅消息/支付收口/发票入口 ✅

| 验收项 | 证据 |
|:---|:---|
| 三标签记录页 | `records.js:5-9` — 在场中 / 待支付（含欠费中 + 可补缴）/ 已完成（含代缴标识） |
| 订阅消息引导 | `pay.js:247` 等 6 个文件 — `wx.requestSubscribeMessage({ tmplIds })` 支付成功后引导 |
| 真实订阅消息发送 | `MiniMessageServiceImpl` — `cgi-bin/message/subscribe/send` 调用 |
| 代缴双推 | 订阅消息推代缴人 + 车主双方 |
| wx.requestPayment 移除 | 全 miniapp 无 `wx.requestPayment` 调用 |
| 发票占位页 | `invoice.wxml` — "功能即将上线" + 禁用表单 |
| 车辆绑定上限 3 | `plate.js:7` — `BIND_LIMIT = 3`，超限提示 |
| 接口路径统一 | /wx/* → /api/v1/mini/* |

**判定**：✅ 完成

---

### 阶段 6：运营端

#### 任务包 6-1：报表中心 + 订单退款 + 菜单修复 ✅

| 验收项 | 证据 |
|:---|:---|
| 收入报表页 | `RevenueReport.vue` — 日报/月报/年报切换 + 车场筛选 + 指标卡片 |
| 车流量报表页 | `TrafficReport.vue` — 进出统计 + 高峰分析 + 车道分布 |
| Apache POI xlsx 导出 | `ReportController.java` — XSSFWorkbook 生成 .xlsx |
| 订单/通行记录 xlsx 导出 | `OrderAdminController.java:386` / `ParkingRecordAdminController.java:243` 升级为 POI |
| 报表聚合 SQL | `ReportMapper.java` — 按天/月/年聚合 + 索引 |
| MENU_MAP 补齐 | `app.ts:51-58` — billing 组含收费规则/费用试算/订单管理/黑白名单/收入报表/车流量报表 |
| 重复页面清理 | parking-lane/parking-zone 逐字节副本已删除 |
| department 孤儿模块 | 已下线 |
| HolidayRuleEditor | 无引用，已删除 |
| 车场图片上传 | 对接真实上传接口 |

**判定**：✅ 完成

---

#### 任务包 6-2：车场参数区 + 名单页 + 账号体系统一 ✅

| 验收项 | 证据 |
|:---|:---|
| 车场参数配置区 | `ParkingLotParamDrawer.vue` — 7 项参数继承/覆盖 + 审核模式开关 |
| 黑白名单页 | `VehicleListManage.vue` — 对接 vehicle_list 实体 + 互斥提示 |
| 月卡/固定车位审核页 | `monthly-pass/index.vue` / `fixed-space/index.vue` — 待审核 tab + 通过/拒绝 |
| 账号体系统一 | `SysAdminAccount` 扩展 — must_change_password + allow_fee_reduction + 多车场关联表 |
| 首次登录强制改密 | `MustChangePasswordInterceptor` — 拦截非改密请求 + `ChangePasswordModal.vue` |
| 随机初始密码 | 创建成功页一次性展示 |
| 旧员工页废弃 | `employee/index.vue` — 警告横幅 "已合并至账号管理"，不可新增/编辑/删除 |

**判定**：✅ 完成

---

## 三、本次审查周期内修复项

在审查过程中发现以下 3 项缺失，已全部修复：

| # | 缺失项 | 所属任务包 | 修复提交 | 状态 |
|:---:|:---|:---|:---|:---:|
| 1 | 车牌校正功能 | 4-1 (BOOTH-005) | b9b41cac~eb578f8d（9 commits） | ✅ 已修复 |
| 2 | 订单详情重算关联展示 | 2-2 (ADMIN-011) | f73bc6c7~9e99e879（4 commits） | ✅ 已修复 |
| 3 | Webhook 签名规范文档 | 0-2 | 9cb10ffd（1 commit） | ✅ 已修复 |

---

## 四、关键指标

| 指标 | 数值 |
|:---|:---|
| 阶段 1~6 任务包总数 | 18 |
| 全部完成任务包 | 18（100%） |
| Flyway 迁移总数 | 75+ 个（含本次新增 V20260902003） |
| 本次审查周期新增提交 | 14 commits |
| 后端编译状态 | BUILD SUCCESS |
| 涉及 Java 文件变更 | ~12 个 |
| 涉及前端文件变更 | ~6 个（booth-web + admin-web） |

---

## 五、阶段 0 审查附注

虽然本次审查范围限定为阶段 1~6，但阶段 0（止血修复）与后续阶段密切相关，简要附注如下：

| 任务包 | 说明 | 状态 |
|:---|:---|:---|
| 0-1 remaining_spaces 断链 + fee_rule 冻结 | remaining_spaces 列已重建（V20260819001），fee_rule 全体系已冻结标注，旧包 package-info 已更新为"本期业务主战场" | ✅ |
| 0-2 Webhook 签名校验 | HMAC-SHA256 校验已实现（WebhookVerificationFilter），但签名规范文档缺失 | ✅ 已修复 |
| 0-3 设备模型解锁双相机 | DROP uq_lane_device_type + recognition_direction / camera_role 字段 + 存量数据回填 + 主备切换逻辑 | ✅ |

---

*审查日期：2026-07-18 | 审查人：AI Orchestrator | 基准代码：develop @ 9cb10ffd*
