# 智慧停车 SaaS 平台 AI 开发协作规范

## 1. 规范地位与术语

本文件是仓库级、工具无关的最高公共协作规范，适用于 Codex、Claude Code、其他编码代理和后续开发人员。用户当前明确指令和经用户确认的当前任务书优先于本文件；仓库中的 README.md、CODEX.md、docs/ 和历史实现只能提供背景，不得扩大本文件确定的系统边界。

本文件使用以下等级：

- 必须（MUST）：不可省略的要求；
- 禁止（MUST NOT）：任何实现不得违反；
- 应（SHOULD）：除非有记录充分的理由，否则应执行；
- 可以（MAY）：结合任务范围选择执行。

规范性业务输入包括：

- docs/需求文档/平台侧需求规格说明书.md；
- docs/需求文档/停车业务平台与DeviceAccess接口规范.md。

文件名带版本号、括号或其他后缀时，必须定位并阅读实际对应文件。若当前检出中找不到规范性输入，涉及该契约的实现必须暂停并报告缺失，不得从旧代码、旧提案或文件名反推接口。Device Access 接口规范中的“当前已实现范围”是当前联调事实；目标设计、后续章节和需求愿景不得被描述成已经可调用。

项目按“从 0 建设停车业务平台侧”的边界推进。仓库中若存在历史 demo、旧设备代码或旧文档，其存在不代表能力已经确认，也不得覆盖本规范。

## 2. 项目定位与系统职责边界

### 2.1 当前开发边界

本仓库只开发智慧停车 SaaS 的停车业务平台侧。

- Device Access 是独立系统，由其他开发人员维护；
- 本仓库禁止修改、复制或嵌入 Device Access 源码；
- 平台禁止直接连接任何厂商设备；
- 平台禁止解析臻识、信路通等厂商协议；
- 平台禁止拼装厂商 MQTT Topic；
- 平台禁止直接调用厂商 SDK、DLL 或 SO；
- 浏览器、管理后台和微信小程序禁止直接调用 Device Access；
- 平台只能由后端通过双方已经确认的接口调用 Device Access；
- 停车、计费、订单、支付和放行决策必须留在平台侧；
- Device Access 不得成为平台业务规则或资金状态的数据源。

需求或目标架构中出现“统一设备适配层、MQTT、厂商 SDK、事件总线”等描述时，必须按当前职责边界解释：厂商协议和设备连接属于 Device Access；平台侧只维护设备业务台账、可信 SN 映射、Device Access 客户端、调用审计和业务决策。

### 2.2 Device Access 当前真实接口

当前已确认且允许联调的接口只有：

    POST /api/v1/devices/{deviceSn}/gate/open
    POST /api/v1/devices/{deviceSn}/time/sync
    GET  /api/v1/devices/{deviceSn}/status

接口规范原始路径占位符可能写作 {deviceId}；其实际值必须是厂商设备 SN。本文使用 {deviceSn} 仅强调值语义，不表示新增或变更接口。

当前行为必须按以下事实处理：

- 开闸和校时均同步等待设备回复；
- Device Access 内部最长等待约 10 秒；
- 当前没有 commandId 幂等、历史命令查询或重复请求结果复用；
- 每次开闸调用都可能执行一次真实动作；
- HTTP 或网络超时不代表道闸一定没有动作；
- 开闸禁止 HTTP 客户端、网关、框架或业务层自动重试和自动重放；
- 网络错误、命令超时或平台客户端读取超时必须标记为 UNCERTAIN；
- UNCERTAIN 不得直接视为成功或失败，必须通过状态查询、现场确认或岗亭人工处置；
- 当前未确认车牌识别事件上报契约；
- 当前未确认 RabbitMQ 设备事件、Exchange、Queue 或 Routing Key；
- 当前未确认 HMAC 服务认证；
- 当前未确认图片上传或补传接口；
- 当前未确认关闸、常开、取消常开、抓拍、重启等控制接口。

凡未出现在接口规范“当前已实现范围”中的能力，一律视为尚未实现。只有 Device Access 已实际实现、双方联调通过并更新冻结规范后，才能进入正式调用链路。

## 3. AI 协作角色

### 3.1 用户

用户是项目负责人和最终决策者，负责：

- 确定产品范围和确认需求；
- 决定是否执行较大改动；
- 决定是否 commit、push、合并和发布；
- 验收最终结果；
- 协调 Device Access 开发人员；
- 提供真机、微信支付、服务器和第三方资料。

AI 禁止替用户作出不可逆业务决策。涉及架构改向、数据迁移、接口冻结、生产发布或资金策略时，必须先取得用户明确决定。

### 3.2 Codex

Codex 主要负责：

- 阅读需求、接口规范和现有代码；
- 项目架构与技术方案设计；
- 数据库设计审查和任务拆分；
- 风险、代码、安全、多租户和数据隔离审查；
- 测试方案、验收标准和回滚方案设计；
- 复核 Claude Code 的实施结果；
- 复杂问题定位；
- 输出下一轮可执行任务书。

Codex 默认不是大范围业务代码的主要实施者。用户明确要求 Codex 直接修改时，Codex 才可实施，并且仍必须小步修改、明确范围、先检查后修改、修改后验证，禁止擅自 commit 或 push。

### 3.3 Claude Code

Claude Code 是主要代码实施者，负责：

- 按经确认的任务书创建或修改模块；
- 实现后端接口、数据库迁移、前端页面和测试；
- 运行构建、测试和必要的人工验证；
- 修复实施过程中发现的局部问题；
- 提交完整实施报告。

Claude Code 禁止擅自改变总体架构、扩大范围、修改冻结接口语义、假设 Device Access 未实现能力、重写无关模块、删除用户代码、修改 Git 历史、commit 或 push。发现方案级问题时，必须停止扩大修改并交由 Codex 或用户决策。

## 4. 标准开发工作流

推荐工作循环：

    用户提出目标
        ↓
    Codex 阅读文档和代码
        ↓
    Codex 进行审查、设计和任务拆分
        ↓
    用户确认范围
        ↓
    Claude Code 按任务书实施
        ↓
    Claude Code 执行构建、测试和人工验证
        ↓
    Claude Code 输出实施报告
        ↓
    Codex 复核代码、测试、安全和需求符合性
        ↓
    Claude Code 根据复核结果进行最小修正
        ↓
    用户最终验收
        ↓
    用户明确授权后才允许 commit / push

简单且风险较低的修复可以缩短设计和复核步骤，但禁止跳过修改前检查、范围控制和修改后验证。

## 5. 任务执行规则

### 5.1 先理解再修改

任何 AI 修改前必须：

1. 阅读任务涉及的需求和接口文档；
2. 阅读相关模块代码；
3. 阅读相关测试；
4. 阅读数据库迁移；
5. 确认真实调用链；
6. 确认租户和停车场数据范围；
7. 确认本次允许与禁止修改的目录；
8. 确认验证方法、验收标准和回滚方式。

禁止仅根据文件名、路由名、类名或历史报告猜测实现。动态状态必须通过当前代码、配置和实际命令重新验证。

### 5.2 控制修改范围

每轮任务应做到：

- 一个明确目标；
- 一个可验证闭环；
- 清楚的允许修改范围；
- 清楚的禁止修改范围；
- 可执行的测试命令；
- 明确的验收标准；
- 可操作的回滚方式。

禁止一次性完成整个停车 SaaS 平台，禁止为了“代码更优雅”修改无关代码，禁止机械创建无业务内容的目录、模块或抽象。

### 5.3 不确定信息

以下信息未由正式资料确认时，禁止编造：

- Device Access 接口和设备能力；
- 厂商协议字段和 MQTT Topic；
- 微信支付参数、商户号、AppId、回调地址和密钥；
- 服务器地址；
- 停车场真实收费规则；
- 真机型号、固件行为和第三方能力。

处理方式必须是：

1. 在不降低安全性的前提下使用接口抽象或配置占位；
2. 写入待确认事项；
3. 明确被阻塞的功能和风险；
4. 继续完成不依赖该信息的部分。

占位不得伪装成已联调能力，Mock 不得被描述成生产能力。

### 5.4 Git 与用户改动保护

执行前必须运行：

    git status
    git branch --show-current

默认禁止：

- commit；
- push；
- force push；
- rebase 用户分支；
- reset --hard；
- 清理或覆盖用户未提交改动；
- 无条件覆盖用户已有文件或其中仍有效的内容；
- 将无关改动纳入本轮结果；
- 修改 Git 历史。

只有用户明确要求时才允许 commit 或 push。发现已有改动时，必须识别归属、绕开无关文件并保留用户工作；无法安全绕开时必须停止并报告。

任务结束前必须重新运行 git status 和 git diff --check，并以实际结果确认修改范围和文本完整性。

## 6. 技术与架构规则

### 6.1 目标技术方向

目标技术栈：

    Java 21 LTS
    Spring Boot 3.5.x
    Maven 多模块
    模块化单体
    MySQL 8.4 LTS
    MyBatis-Plus
    Flyway
    Redis
    Sa-Token
    RabbitMQ
    WebSocket
    对象存储
    Docker Compose
    Nginx

    Vue 3
    TypeScript
    Vite
    Pinia
    Vue Router
    Arco Design Vue

    原生微信小程序

以上是目标方向，不得写成当前仓库已经完成的事实。实际版本最终以经确认的架构文档、pom.xml、package.json 和锁文件为准；上述来源不一致时必须报告并由用户确认迁移任务。

未经评审禁止改成微服务、引入 Kubernetes、更换主框架或数据库、引入复杂领域框架，或创建大量无实现价值的空壳模块。

### 6.2 模块化单体

第一阶段必须使用模块化单体：

- 模块之间通过公开 Service、Facade 或领域事件协作；
- 禁止跨模块直接操作对方 Mapper；
- 禁止跨模块直接修改对方数据库实体；
- 禁止 Controller 承载复杂业务逻辑；
- 禁止 Controller 直接编写复杂查询和状态更新；
- 禁止循环依赖；
- 禁止支付模块调用 Device Access；
- 禁止设备模块直接修改支付状态；
- 停车业务决策必须留在平台侧。

平台设备模块只负责平台设备台账、绑定关系、可信 SN 映射、Device Access Client、调用审计和状态展示；禁止演变为厂商协议适配器。

### 6.3 推荐分层

业务模块可按复杂度使用：

    controller
    application
    domain
    infrastructure
    mapper
    entity
    dto
    vo
    convert
    event
    config

不得机械创建空目录。职责应保持：

- Controller：参数接收、鉴权入口、响应转换；
- Application Service：用例编排和事务边界；
- Domain Service：核心业务规则；
- Repository/Mapper：数据访问；
- Client/Gateway：外部系统调用；
- DTO：跨层或跨系统数据；
- Entity：数据库映射；
- Domain Model：业务状态和行为。

## 7. 数据库规则

- 数据库变更必须使用 Flyway；
- 已发布迁移禁止直接修改；
- 每次数据库变更使用独立版本脚本；
- 生产迁移前必须备份并准备回滚；
- 所有核心表必须设计必要的唯一约束和索引；
- 所有租户业务查询必须执行租户过滤；
- 停车场级业务必须执行停车场过滤；
- 禁止只依赖前端传入的 tenantId 或 parkingLotId；
- 金额必须统一为整数分，或采用经架构确认的精确 Decimal；接口、数据库和计算规则不得混用单位；
- 订单、支付、停车记录的金额和状态变更必须使用精确字段更新；
- 禁止使用携带未知字段的整实体 updateById 修改金额或状态；
- 关键并发操作必须使用乐观锁、唯一约束或带状态条件的精确更新；
- 迁移必须验证前向执行、重复执行保护、索引约束和回滚方案。

## 8. 多租户与权限 P0 红线

以下要求全部为高优先级红线，任何违反均视为 P0：

1. 客户只能访问本租户数据；
2. 停车场人员只能访问授权停车场；
3. 设备必须绑定租户、停车场和车道；
4. 所有权限必须由后端校验；
5. 前端隐藏菜单不能代替鉴权；
6. 禁止直接信任前端传入的租户 ID；
7. 禁止直接信任前端传入的停车场 ID；
8. 禁止直接信任前端传入的设备 SN；
9. 非法数据范围必须失败关闭；
10. 定时任务、消息消费和 WebSocket 推送也必须执行数据隔离；
11. 超级管理员代客户操作必须记录真实操作人、目标租户和目标停车场；
12. 跨租户、跨停车场测试必须作为验收内容。

所有查询、详情、导出、修改、删除、统计、批处理和异步处理均适用，不得只保护列表接口。

## 9. Device Access 接入红线

- 平台数据库必须保留自己的设备主键；
- 厂商 SN 必须单独保存为 device_sn；
- 当前调用 Device Access 时使用可信记录中的 device_sn；
- 禁止前端自由输入 SN 后直接调用；
- 后端必须根据平台设备主键查询可信 SN；
- 调用前必须校验权限、租户、停车场、车道、设备启用状态和能力范围；
- 开闸前必须保存或准备可持久化的设备调用审计；
- 开闸禁止任何形式的自动重试、透明重试和消息重放；
- 网络错误或超时必须标记为 UNCERTAIN；
- UNCERTAIN 禁止直接改写为成功或失败，必须经状态查询、现场确认或岗亭人工处置；
- Device Access 当前应部署在可信内网并限制调用源；
- 浏览器和小程序禁止直接调用 Device Access；
- 当前未确认的 HMAC 禁止由平台单方面发送并假设服务端支持；
- 禁止将目标设计中的 RabbitMQ、统一命令、图片或其他设备能力接入当前生产调用链。

## 10. 支付与资金安全红线

- 金额统一使用整数分或经架构确认的精确 Decimal；
- 禁止使用 float、double 或前端浮点数处理资金；
- 支付结果必须以服务端主动查询和验签回调为准；
- 支付回调必须验签；
- 必须校验 appid、mchid、outTradeNo 和金额；
- 支付回调必须幂等；
- 重复回调禁止重复更新订单或重复触发资金动作；
- 支付成功与开闸必须解耦，并具备补偿、人工兜底和审计；
- 零元订单必须有明确、可追溯的状态流转；
- 负金额必须拒绝；
- 退款金额不得超过实际支付金额；
- 日志禁止记录支付密钥、完整签名、证书私钥和敏感凭证；
- 日志禁止输出密码、Token、身份证号、手机号等敏感信息明文；
- 任何支付链路修改必须包含正常、重复、伪造、金额不一致和异常补偿测试。

## 11. 测试与验收

任何代码任务完成后，必须按修改范围执行相应验证，最低应覆盖：

- 编译；
- 单元测试；
- 相关模块测试；
- 数据库迁移检查；
- 接口测试；
- 权限测试；
- 数据隔离测试；
- 幂等测试；
- 异常路径测试。

涉及 MySQL、Redis、RabbitMQ 等基础设施时，应优先使用 Testcontainers。涉及 Device Access 时，必须使用 WireMock、MockWebServer 或项目已经确认的等价 HTTP Mock 工具，并至少覆盖：

    HTTP 200 成功
    404 设备不存在
    503 MQTT 不可用
    500 设备错误
    约 10 秒命令超时
    平台客户端超时
    响应解析失败
    跨停车场调用
    重复开闸调用
    UNCERTAIN 状态
    审计记录

禁止只验证正常路径。重复开闸测试必须证明平台没有自动重试，不能通过真机重复动作冒险完成。

测试命令必须来自当前仓库真实的构建文件、脚本和锁文件。若某项测试因环境、真机、支付资料或第三方服务无法运行，完成报告必须列出：

- 未运行的测试；
- 原因；
- 风险；
- 后续验证命令或人工步骤。

禁止使用“理论上可以”代替验证，也禁止把旧验证报告当作当前命令结果。

## 12. 文档维护

以下变化必须同步审查或更新 AGENTS.md 和 CLAUDE.md：

- 技术栈发生变化；
- 模块边界发生变化；
- Device Access 接口正式升级；
- 车牌识别事件契约冻结；
- 支付模式发生变化；
- Git 工作流发生变化；
- Codex 和 Claude Code 职责发生变化；
- 测试或部署方式发生变化。

接口能力只有在实现、联调和规范三者一致后，才可从“待确认”改为“当前已实现”。文档结论必须基于真实文件、接口、代码、迁移或执行命令，禁止编造截图、测试、部署和生产能力。

启动方式、公共接口、数据库结构、架构决策或验证结论变化时，应同步评估相关 README、ADR 和验收文档；若不在当前允许修改范围内，必须列为待办，不得擅自扩大本轮范围。

修改协作规范本身时，报告必须说明：

1. 修改原因；
2. 影响范围；
3. 是否改变现有工作流；
4. 是否需要更新其他文档。

## 13. 任务开始与完成格式

复杂任务开始时，实施者必须先输出：

    目标
    允许修改范围
    禁止修改范围
    计划步骤
    验证方式
    已识别风险

简单任务不需要冗长计划，但仍必须确认边界和验证方式。

任务完成时必须输出相同的十项报告：

    1. 完成内容
    2. 修改文件
    3. 数据库变更
    4. 接口变更
    5. 测试和验证结果
    6. 未完成内容
    7. 已知风险
    8. 待确认事项
    9. 建议下一步
    10. Git 状态

不得只输出“已完成”。没有数据库或接口变更时也必须明确写“无”。

## 14. P0 禁止项汇总

出现以下任一情况，任务不得验收或进入联调：

1. 越权、跨租户或跨停车场访问未失败关闭；
2. 平台直连厂商设备、解析厂商协议、拼装厂商 Topic 或调用厂商 SDK；
3. 修改 Device Access 源码或绕过已确认接口；
4. 将目标设计或未冻结契约当作当前真实能力；
5. 直接信任前端租户 ID、停车场 ID 或设备 SN；
6. 自动重试开闸，或把超时和网络错误直接判定为未动作；
7. 开闸无权限校验、数据范围校验或审计；
8. 支付回调未验签、未校验身份金额、无幂等或使用浮点金额；
9. 泄露密钥、证书、Token、完整签名、密码或敏感个人信息；
10. 未经用户授权执行 commit、push、破坏性 Git 操作或覆盖用户改动。
