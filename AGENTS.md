# AGENTS.md — 停车SaaS系统

> 权威需求来源: `docs/需求规格说明书_v1.2.md`

## 项目概述
多租户停车SaaS管理平台，支持运营端(Web)、岗亭端(Web)、小程序端(微信)三端，通过适配器层(Device Access)对接停车场硬件设备（相机、道闸等）。

**本期范围**: 核心进出场、计费引擎、订单、模拟支付、设备管理、车场/车道、月卡/固定车位、系统参数、岗亭监控、交接班、报表。
**本期不做**: 优惠券/积分、访客预约、商家优惠、真实支付对接、电子发票真实开具、寻车导航、短信推送、岗亭离线操作、地感对接、支付宝小程序。完整清单见需求文档第8章。

## 系统架构

```
用户层:  运营端(Web)  +  岗亭端(Web)  +  小程序端(微信)
              │                │  WebSocket(实时推送)
              └────────────────┴──────────────┘
                               │ HTTPS / WSS (公网仅开放 443)
┌──────────────────────────────┼──────────────────────────────┐
│                     云端服务器（全云端部署）                  │
│                 ┌───────────┴───────────┐                   │
│                 │    平台服务层          │                   │
│                 │   (Java/Spring)       │                   │
│                 └───────────┬───────────┘                   │
│                             │ HTTP REST / Webhook(内网回环)   │
│                 ┌───────────┴───────────┐                   │
│                 │      适配器层          │                   │
│                 │   (Device Access)     │                   │
│                 │    HTTP ↔ MQTT        │                   │
│                 └───────────┬───────────┘                   │
│                 ┌───────────┴───────────┐                   │
│                 │  EMQX (MQTT Broker)   │← MQTTS 8883 ──────│
│                 └───────────────────────┘                   │
│                 ┌───────────────────────┐                   │
│                 │     流媒体网关         │← GB28181 推流 ────│
│                 │ GB28181 → WebRTC      │── WebRTC ─────────│──→ 岗亭端浏览器
│                 └───────────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
                               │
                   MQTT Client主动外连 / GB28181主动推流
                               │
                    设备层(相机 / 道闸 / GPIO)
                    （车场现场，NAT后，无边缘设备）
```

**通信链路**:
| 方向 | 协议 | 用途 |
| :--- | :--- | :--- |
| 平台 → 适配器 | HTTP REST（内网回环） | 设备注册、开闸、关闸、校时、状态查询 |
| 适配器 → 平台 | HTTP Webhook（内网回环） | 识别事件、设备状态上报（签名校验） |
| 适配器 ↔ 设备 | MQTT 3.1.1 (EMQX) | 设备控制、状态上报（相机经公网MQTTS主动外连） |
| 平台 → 岗亭端 | WebSocket（WSS） | 远程开闸弹窗、设备告警、异常推送 |
| 相机 → 流媒体网关 | GB28181 | 相机主动推流至云端流媒体网关 |
| 岗亭端 → 流媒体网关 | WebRTC | 视频预览（≤500ms延迟） |

**关键架构决策**（V1.2 修订）:
- GPIO控制统一经"平台→适配器→MQTT→设备"链路，**平台不直连EMQX**（废止v0.2过渡方案）
- **适配器与平台同机云端部署**，走内网回环（127.0.0.1），不再要求适配器公网可达
- 流媒体网关**云端部署**，相机通过 **GB28181 主动推流**至云端，转封装为 WebRTC 供浏览器播放（替代原"现场拉RTSP"方案；不支持GB28181的相机按 BOOTH-003 降级方案处理）
- 现场无需工控机/边缘设备，相机经路由器NAT主动外连云端
- 公网仅暴露 **443（HTTPS/WSS）** 和 **8883（MQTTS）** 两个端口
- 现场降级: 平台不可达时，相机本地白名单（适配器经云端MQTT定期同步月卡/固定车位/白名单车牌）可自动放行，临停车辆由现场人工处理

## 角色与权限

| 角色 | 创建方式 | 可见范围 | 终端 |
| :--- | :--- | :--- | :--- |
| 超级管理员 | 系统预设 | 全平台 | 运营端 |
| 租户管理员 | 仅超级管理员创建 | 被分配的车场 | 运营端 |
| 岗亭管理员 | 仅超级管理员创建并分配车场 | 被分配的车场 | 岗亭端 |
| 小程序用户 | 微信授权+手机号绑定 | 本人车辆 | 小程序端 |

- 岗亭管理员不隶属特定租户，可跨租户分配车场（不构成租户数据越权）
- 租户管理员远程开闸权限默认关闭，需超级管理员手动开启
- 岗亭管理员费用减免权限需超级管理员创建时勾选

## 功能编号体系
所有功能需求对应编号，代码中标注便于溯源：
- **ADMIN-xxx**: 运营端（数据看板、车场/设备管理、收费规则、订单、报表、系统参数等，共22项）
- **BOOTH-xxx**: 岗亭端（登录、监控、通行作业、收费、交接班等，共10项）
- **MINI-xxx**: 小程序端（登录、临停缴费、代缴、月卡、车辆管理、余位查询等，共11项）
- **COMM-xxx**: 平台-适配器-设备通信（命令下发、事件上报、MQTT，共3项）

## 核心业务规则

### 订单状态机
```
预订单 ──出场计算金额──→ 待支付 ──支付成功──→ 已支付 ──出场完成──→ 已完成
  │                        │                     │
  │                        ├─超时关闭──→ 已取消    └─退款(模拟)──→ 已退款
  │                        │ (再次出口识别: 重新计费生成新待支付订单)
  │                        └─允许欠费──→ 欠费中 ──补缴──→ 已完成
  └─免费放行──────────────────→ 已完成
```
- 仅临停车辆入场时生成预订单；月卡/固定车位/白名单仅生成通行记录，不生成订单
- 出场时预订单更新为待支付并计算金额

### 计费规则
- **免费时长**: 入场标记免费，超期后从免费时长结束后开始计费（例: 前15分钟不计入，第16分钟起计费）
- **跨天计费**: 可配置（按自然日分段 / 连续计费）
- **封顶计费**: 24小时封顶
- **时段阶梯**: 不同时段不同费率
- **生效方式**: 立即生效 / 仅新入场生效 / 定时生效
- **配置粒度**: 车场级为基础，支持区域/通道差异化覆盖

### 开闸规则
- **自动开闸条件**: 月卡/固定车位有效期内、白名单、已支付订单（出场窗口期内）、免费放行
- **支付出场窗口期**: 出口缴费（车辆已在出口）支付成功立即开闸；提前缴费后进入窗口期（默认15分钟），窗口期内出口识别自动开闸；超期则重新计费生成新订单
- **未支付拦截**: 按车场配置（拦截不开闸 / 允许欠费出场）
- **欠费追缴**: 欠费中订单在小程序"待支付"可补缴；欠费车辆再次出场按车场配置（默认必须补缴）
- **开闸幂等性**: 重复下发同一指令直接响应，无副作用

### 代缴规则
- 任何人输入任意车牌都能代缴
- 支付记录归实际支付人
- 支付成功通知推送代缴人和车主双方

### 月卡
- 不绑定车位号，仅保证入场权限
- 状态: 生效中 → 已过期/已注销
- 到期自动失效，按临停计费
- 小程序申请 → 运营端审核（可配置自动通过/需审核） → 审核通过后小程序内模拟支付 → 生效

### 固定车位
- 绑定具体车位号，专属此车（1:1）
- 系统仅记录绑定关系，不校验实际物理占用
- 到期管理与月卡分开

### 黑白名单
- 互斥（同一车牌不能同时在黑名单和白名单中）
- 黑名单触发模式可配置: 禁止入场 / 允许入场但告警 / 按类型区分（欠费类/管理类/其他类）
- 白名单车辆自动放行，不计费

### 无牌车
- 岗亭手动录入临时车牌，系统按临时车牌生成预订单
- 全程按临时车牌参与计费、匹配与记录

### 数据隔离
- 超级管理员: 全平台数据
- 租户管理员: 仅被分配车场数据
- 岗亭管理员: 仅被分配车场数据（不隶属特定租户）
- 小程序用户: 仅本人绑定车辆数据

## 包架构说明
- **com.jushan.system** — 本期业务主战场，受控使用。承载进出场、计费引擎、订单、模拟支付、设备、车场/车道、月卡/固定车位、系统参数、岗亭监控等约 80% 核心业务。V1.1 新功能需对应功能编号标注，二期统一迁移至 com.jushan.platform.modules。
- **com.jushan.platform.modules** — 已成型模块（auth/account/company/department、device-webhook、miniapp、parking-session/recognition-event）。
- **com.jushan.platform.modules.parking** — fee_rule 计费体系代码已实现但本期冻结为二期候选。本期以旧 billing_rule 为唯一计费体系。

## 技术栈
- 后端: Java 21 + Spring Boot 3.x + MyBatis-Plus + MySQL 8 + Flyway
- 前端: Vue 3 + Ant Design Vue (admin-web/booth-web)
- 小程序: 微信小程序原生（基础库 ≥ 2.19.0）
- 通信: HTTP REST (平台↔适配器) + MQTT (适配器↔设备) + WebSocket (平台→岗亭端) + WebRTC (流媒体网关→岗亭端)
- 中间件: RabbitMQ (平台内部事件总线) + Redis (缓存)

## 构建命令
```bash
# 后端编译
mvn clean compile -pl parking-system -am

# 后端测试
mvn test -pl parking-boot -am

# 后端打包
mvn clean package -pl parking-boot -am

# admin-web 构建
cd admin-web && pnpm build

# booth-web 构建
cd booth-web && pnpm build

# 小程序构建
cd miniapp && npm run build:mp-weixin
```

## 模块结构
```
parking-boot/          # 启动模块 + Flyway 迁移 + 集成测试
├── parking-system/    # 业务模块 (~13,500+ 行)
├── parking-infrastructure/  # 安全/JWT/权限/租户/日志
├── parking-framework/ # Redis/MQ/WebSocket/分布式锁
└── parking-common/    # BaseEntity/R/ErrorCode
admin-web/             # 运营端 Vue 3
booth-web/             # 岗亭端 Vue 3
miniapp/               # 微信小程序
```

## 关键文件
- `parking-boot/src/main/resources/application.yml` — 主配置（pyun.mock 必须 true）
- `parking-system/src/main/java/com/jushan/system/service/MockPaymentService.java` — 模拟支付核心
- `parking-system/src/main/java/com/jushan/system/service/EntryService.java` — 入场核心
- `parking-system/src/main/java/com/jushan/system/service/ExitService.java` — 出场核心
- `parking-infrastructure/.../TenantLineInnerInterceptor.java` — 多租户拦截器
- `docs/需求规格说明书_v1.2.md` — 权威需求文档

## 架构约束

### 多租户
- 所有业务表必须含 tenant_id，MyBatis-Plus TenantLineInnerInterceptor 自动拦截
- 超级管理员全平台，租户管理员仅被分配车场，岗亭管理员仅被分配车场
- 岗亭管理员不隶属特定租户，按被分配车场授权，不视为租户数据越权

### 通信
- 平台→适配器走 HTTP REST（命令下发，内网回环，同机部署不暴露公网），适配器→设备走 MQTT
- 适配器→平台走 HTTP Webhook（事件上报，需签名校验，内网回环）
- 平台→岗亭端走 WebSocket（实时推送）
- **GPIO控制统一经适配器转发，平台不直连EMQX**
- **EMQX 云端部署，相机经公网 MQTTS（8883）主动外连，必须启用 TLS + 设备级 ACL（SEC-012）**
- 公网仅暴露 443（HTTPS/WSS）与 8883（MQTTS）

### 支付
- 本期仅模拟支付，禁止连接任何真实支付平台
- MockPaymentService 是唯一支付入口
- 待支付订单超时关闭后，再次出口识别时重新计费生成新订单

### 设备
- 开闸/关闸/校时通过 DeviceAccessClient HTTP REST 下发
- 事件通过 Webhook 接收
- 支持单相机模式和双相机模式（主备切换）
- 双向通道须分别绑定入场/出场方向识别相机
- 远程校时: 平台 → HTTP REST → 适配器 → MQTT → 设备

### 视频预览
- 相机RTSP流浏览器无法原生播放
- 流媒体网关部署于云端，相机通过GB28181主动推流至云端，转封装为WebRTC
- 延迟要求 ≤ 500ms
- 不支持GB28181的相机按 BOOTH-003 降级方案处理

### 现场降级 (REL-006)
- 平台不可达时，适配器定期同步至相机本地白名单的月卡/固定车位/白名单车辆可自动放行
- 临停车辆由现场人员人工处理，平台恢复后补录数据

## 代码规范
- 所有实体类必须含 tenant_id, created_at, updated_at, deleted_at
- 金额统一用 BigDecimal（数据库 DECIMAL(10,2)），禁止 float/double
- 时间统一用 LocalDateTime，禁止 java.util.Date
- 所有 Controller 必须加 @RequirePermission 权限注解
- 敏感操作（开闸/收费规则修改/手动开闸）必须加 @BusinessLog 记录操作日志
- 数据库变更必须用 Flyway 迁移，命名 V{日期序号}__{描述}.sql
- 接口返回统一格式 {code, message, data, timestamp}
- 异常统一全局处理，Controller 中禁止写 try-catch

## 安全红线
- 禁止生产环境加载 InternalGateController（已标记 @Profile("dev")）
- 禁止 pyun.mock=false（真实支付已物理切断）
- 禁止前端传入 deviceSn 直接操作设备，必须通过 laneId 查询数据库可信记录
- 禁止未授权访问其他租户数据，所有查询必须带 tenant_id 或 ParkingLotScopeResolver
- 禁止在 Controller 中写 SQL 拼接，所有查询通过 MyBatis-Plus 或 @Query JPQL
- Webhook请求必须签名验证，防伪造
- 登录失败锁定: 连续5次失败锁定账号15分钟
- 首次登录强制修改初始密码
- JWT: 访问令牌2小时，刷新令牌7天
- MQTTS 设备接入安全（SEC-012）: 相机公网连接须 TLS 加密 + 设备级 ACL，每台设备仅可收发自身 Topic

## 测试要求
- 单元测试覆盖率 ≥ 70%（核心业务模块）
- 新增功能必须附带单元测试
- 提交前必须运行 mvn test 确保通过
- 已有失败测试（RecognitionEventServiceImplTest、DeviceWebhookControllerTest）为已知问题，不阻塞提交

## 常用数据字典
- **订单状态**: 1=预订单, 2=待支付, 3=已支付, 4=已退款, 5=欠费中, 6=已完成, 7=已取消
- **订单类型**: 1=临停订单, 2=月卡订单, 3=固定车位订单
- **支付方式**: 1=模拟支付(自缴), 2=模拟支付(代缴), 3=现金, 4=免费放行
- **月卡状态**: 1=生效中, 2=已过期, 3=已注销
- **通道类型**: 1=入口, 2=出口, 3=双向
- **角色类型**: 1=超级管理员, 2=租户管理员, 3=岗亭管理员, 4=小程序用户
- **黑白名单**: 1=黑名单, 2=白名单
- **黑名单触发类型**: 1=欠费类, 2=管理类, 9=其他类
