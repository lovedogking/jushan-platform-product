# 返修报告（联合验收 A5 / A6 / E7）

## 总览

| 编号 | 问题 | 状态 | 修改文件数 |
|------|------|------|-----------|
| A5 | register.ts 双前缀与风格统一 | ✅ 已修复 | 1 |
| A6 | V1.1 需求文档入库 + 指针更新 | ✅ 已修复 | 4 + 1 新增 |
| E7 | 收入统计排除已退款订单 | ✅ 已修复 | 2 |

后端编译 `mvn compile -pl parking-system -am` 通过，无新增错误。

---

## 修复 1（A5）：register.ts 双前缀与风格统一

### 修改文件

`admin-web/src/api/register.ts`

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| 1 | `import axios from 'axios'` | `import request from '@/utils/request'` |
| 12 | `return axios.post('/api/v1/register', data)` | `return request.post('/v1/register', data)` |

### 自验

- `import` 已改为 `request`，与同目录 `fee-rule.ts`、`fee-calculation.ts`、`department.ts` 风格一致
- 请求路径 `/v1/register`，叠加 `request` 的 `baseURL='/api'` 后实际请求为 `/api/v1/register`，与后端 `SecurityConfig` 中 `permitAll()` 的白名单 ``/api/v1/register` 精确匹配
- 同文件无其他端点
- 编译通过

---

## 修复 2（A6）：V1.1 需求文档入库 + 指针更新

### 新增文件

| 文件 | 说明 |
|------|------|
| `docs/Kimi_Agent_停车SaaS架构评审/停车SaaS系统需求规格说明书_v1.1.md` | V1.1 需求规格说明书（80KB） |

### 修改文件

**CLAUDE.md:34**
```
- 需求文档：`docs/requirements/停车SaaS系统需求规格说明书_v1.0.md`
+ 需求文档：`docs/Kimi_Agent_停车SaaS架构评审/停车SaaS系统需求规格说明书_v1.1.md`
```

**README.md:7**
```
- 需求文档：`docs/requirements/停车SaaS系统需求规格说明书_v1.0.md`
+ 需求文档：`docs/Kimi_Agent_停车SaaS架构评审/停车SaaS系统需求规格说明书_v1.1.md`
```

**docs/README.md:9-10**
```
| `requirements/` | 需求规格说明书 | `停车SaaS系统需求规格说明书_v1.0.md` |
（替换为）
| `requirements/` | 需求规格说明书 | `停车SaaS系统需求规格说明书_v1.0.md`（历史存档） |
| `Kimi_Agent_停车SaaS架构评审/` | V1.1 需求规格说明书 | `停车SaaS系统需求规格说明书_v1.1.md` |
```

### 自验

- V1.1 文档已放入 `docs/Kimi_Agent_停车SaaS架构评审/` 目录（文件名：`停车SaaS系统需求规格说明书_v1.1.md`，80KB）
- CLAUDE.md、README.md、docs/README.md 指针全部指向 V1.1 路径
- v1.0 原文档保留于 `docs/requirements/` 作为历史存档
- 全局搜索 `"需求文档.*v1.0"` 无残留活指针（仅 `docs/README.md` 中标注为"历史存档"）

---

## 修复 3（E7）：收入统计排除已退款订单

### 修改文件

**1. `parking-system/src/main/java/com/jushan/system/mapper/DashboardMapper.java`**

四处 SQL 增加 `AND status != 'REFUNDED'`：

| 方法 | 行号 | 修改 |
|------|------|------|
| `sumTodayRevenue()` | 28 | WHERE 追加 `AND status != 'REFUNDED'` |
| `sumTodayRevenueByLots()` | 36 | WHERE 追加 `AND status != 'REFUNDED'` |
| `selectHourlyRevenue()` | 145 | WHERE 追加 `AND status != 'REFUNDED'` |
| `selectHourlyRevenueByLots()` | 154 | WHERE 追加 `AND status != 'REFUNDED'` |

每处上方添加注释：
```java
/**
 * 收入口径：排除 REFUNDED（任务包 1-2 / 6-1 共用）。
 */
```

**2. `parking-system/src/main/java/com/jushan/system/service/ParkingOrderService.java:657`**

`isCountedInRevenue()` 注释更新，注明 DashboardMapper 已同步：
```java
已退款（REFUNDED）订单不计入实付收入。
DashboardMapper 金额类查询已同步加 status != 'REFUNDED'，任务包 6-1 收入报表统一使用本口径。
```

### 核查范围

- `sumTodayRevenue()` / `sumTodayRevenueByLots()` — 已修复
- `selectHourlyRevenue()` / `selectHourlyRevenueByLots()` — 已修复
- `countTodayEntry/Exit/Parking` — 车流量统计，不受影响（查 `parking_record` 表，不涉及订单金额）
- `countEnabledDevices/countOnlineDevices/countUnhandledAlerts` — 设备/异常统计，不受影响
- `sumRemainingSpaces` — 余位统计，不受影响
- `selectHourlyEntry/Exit` — 车流量趋势，不受影响

### 自验

- 后端编译 `mvn compile -pl parking-system -am` 通过
- 4 处金额类 SQL 均已添加 `AND status != 'REFUNDED'`，车流量/设备/余位查询不受影响
- `isCountedInRevenue()` 与 `DashboardMapper` SQL 口径一致
- 注释标注 `任务包 1-2 / 6-1 共用`，后续任务包 6-1 可复用

---

## 验收对照

| 原验收编号 | 原判定 | 修复后 | 证据 |
|-----------|--------|--------|------|
| A5 | ⚠️WARN | ✅PASS | register.ts:1→`import request`，line 12→`/v1/register` |
| A6 | ⚠️WARN | ✅PASS | V1.1 文档入库；CLAUDE.md:34 / README.md:7 / docs/README.md 指针全部更新 |
| E7 | ⚠️WARN | ✅PASS | DashboardMapper 4 处金额 SQL 加 `status != 'REFUNDED'`，编译通过 |

---

*报告生成时间：2026-07-17*
*修改范围：6 个已修改文件 + 1 个新增文档*
