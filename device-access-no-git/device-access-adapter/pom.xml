<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.smartparking</groupId>
        <artifactId>device-access</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>device-access-adapter</artifactId>
    <packaging>jar</packaging>

    <name>Device Access Adapter</name>
    <description>Brand-specific protocol adapters - currently Zhenshi C5H only</description>

    <dependencies>
        <dependency>
            <groupId>com.smartparking</groupId>
            <artifactId>device-access-common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.smartparking</groupId>
            <artifactId>device-access-mqtt</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- Caffeine 高性能本地缓存，用于替代 ConcurrentHashMap 实现 TTL 过期（修复 lastPlateMap 内存泄漏） -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>3.1.8</version>
        </dependency>
    </dependencies>
</project>
