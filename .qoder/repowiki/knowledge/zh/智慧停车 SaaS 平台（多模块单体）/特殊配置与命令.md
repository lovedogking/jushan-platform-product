仓库根执行 `./mvnw clean install` 一次性构建全部 Java 模块；`./mvnw spring-boot:run -pl parking-boot -am` 仅启动 parking-boot 并自动拉起其依赖的前置模块；`docker compose up -d` 启动 infra 中的 Nginx 与 MySQL 占位服务，配合 .env.example 配置本地开发环境。