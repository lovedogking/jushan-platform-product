- Nginx 配置采用「主配置 + conf.d/*.conf」分层，所有站点路由集中在 default.conf，主配置仅保留全局日志、性能与 gzip 参数。
- 所有反向代理 location 块统一设置 Host、X-Real-IP、X-Forwarded-For、X-Forwarded-Proto 四个上游透传头，并在请求头中透传 X-Trace-Id 以贯穿链路追踪。
- Codex agent 与 skill 均遵循「描述性 TOML/YAML + SKILL.md」双文件结构，agent 通过 config_file 字段引用同目录下的独立配置文件。