- 每个子域遵循 controller → service → mapper → entity 四层结构，请求/响应对象分别放在 dto/ 与 vo/ 下，避免直接暴露 entity。
- 对外 API 统一使用 RESTful 风格，参数校验注解与 Sa-Token 权限注解置于 Controller 层，业务逻辑下沉至 Service。
- 跨子域共享的 DTO/VO 仅保留字段定义，不包含业务方法，保持无状态。