- 所有 Java 模块继承 Spring Boot Parent 并通过根 pom.xml 的 properties 与 dependencyManagement 统一管理第三方依赖版本，禁止子模块自行声明版本号
- 后端分层严格遵循 controller/service/mapper/vo 四段式，业务实体与外部 DTO 通过 MapStruct 转换，Lombok 用于样板代码生成
- 统一通过 parking-common 的 R<T> 响应体与 BusinessException + CommonErrorCode 返回错误，不直接抛出原始异常