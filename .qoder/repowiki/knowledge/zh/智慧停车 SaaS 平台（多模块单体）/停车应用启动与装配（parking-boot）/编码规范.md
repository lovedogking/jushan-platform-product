- 数据库迁移脚本统一采用 `VYYYYMMDDNNN__描述.sql` 命名规范，按时间顺序递增，禁止修改已提交版本。
- 配置按 profile 拆分到 `application-{profile}.yml`，敏感信息只允许通过环境变量注入，禁止写入共享 `application.yml`。
- 可选外部依赖（DataSource、RedisConnectionFactory）一律通过构造器注入 `ObjectProvider<T>` 并在运行时判断 null 以优雅降级。
- 条件化装配使用 `@ConditionalOnProperty(name = "jushan.*", havingValue = "true", matchIfMissing = true)` 模式，默认开启但支持显式关闭。
- 健康检查按 Txx 阶段注释标注当前能力范围，新增下游连通性检查时沿用 `builder.withDetail(key, value)` 追加详情的写法。