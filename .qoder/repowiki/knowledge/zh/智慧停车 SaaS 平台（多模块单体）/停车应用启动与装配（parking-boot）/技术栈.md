Spring Boot 3 + MyBatis-Plus（jsqlparser 分页 COUNT）、Flyway（MySQL 迁移）、Sa-Token（Header-only Token 读取）、Micrometer Prometheus Registry、Spring Boot Actuator（readiness/liveness probes）、WireMock（HTTP Mock）、Testcontainers（MySQL/RabbitMQ）。