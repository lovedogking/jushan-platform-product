# 界面优化调整提示词（基于当前版本微调，保留正确结构，只做细节优化，直接复制发送）
## 一、必须保留的已有正确内容
1. 整体布局：左侧四宫格主内容区 + 右侧通高侧边栏的结构不变
2. 配色风格：浅灰白色页面背景、白色卡片、细灰色分割线的浅色B端风格不变
3. 按钮配色：手动放行（浅灰边框）、道闸常开（蓝色实心）、关闸（红色实心）、开闸（绿色实心）的配色规则不变
4. 四大功能模块：入口车道控制、出口车道控制、车辆详情、场内记录表格的模块划分不变
5. 右侧侧边栏的车位统计、提示消息模块保留

## 二、具体优化调整指令
### 1. 顶部导航栏重构（重点修正）
- 顶部做成双层通栏导航：
  - 第一层（主导航）：最左侧文字「飓山智慧停车 岗亭工作区」+ 绿色圆形“在线”标识；紧邻右侧是标准下拉选择框，默认选中「测试车场」；最右侧依次排列：系统时间、账号名「gangting001」、「退出」按钮
  - 第二层（功能按钮栏）：横向均匀排列10个功能按钮，每个为「小图标+文字」样式，从左到右依次是：刷新设备、车辆查询、通行记录、缴费记录、场内记录、车道监控、全屏显示、车道事件、呼叫终端、修改余位
- 修正当前顶部简陋的下拉框样式，做成标准后台下拉组件

### 2. 四宫格模块细节打磨
- 【上排两个车道卡片】：视频区域保持浅灰底色+居中摄像机图标+“视频接入中，敬请期待”文字；底部操作按钮与卡片底边保留均匀内边距，按钮尺寸统一、间距均等；卡片顶部的车道下拉框靠左，绿色“在线”标签靠右对齐
- 【左下车辆详情卡片】：重排信息布局，左侧车辆抓拍图垂直居中；右侧信息分两列工整排列，左列为属性标签（车牌、车道、时间、金额、备注），右列为对应数值，每行水平对齐，增加合理行间距，移除杂乱的虚线分割线
- 【右下场内记录卡片】：收紧表格行高，内容更紧凑；顶部的「仅查询无牌」复选框、车牌搜索框、「查询」按钮横向紧凑排列，整体靠右对齐；表格内的入场图片缩略图尺寸统一

### 3. 右侧侧边栏优化
- 适当加宽侧边栏宽度，内容左右留白更合理
- 车位统计区域：标签左对齐、数值右对齐，三行（总车位、在场车辆、剩余车位）清晰分隔，排版工整
- 下方“暂无消息”占位图标+文字居中显示，样式统一

### 4. 补充底部状态栏
- 页面最底部增加一条全宽度的浅灰色窄状态栏

### 5. 整体质感统一
- 所有卡片圆角统一，内边距一致，模块间距均匀
- 字体层级清晰：模块标题加粗，正文常规字重，辅助信息用浅灰色
- 全局对齐工整，符合专业停车场SaaS后台的简洁专业质感，无多余装饰

## 三、禁止改动的红线
1. 不允许改回深色视频背景
2. 不允许打乱四宫格结构
3. 不允许新增未提及的功能模块
4. 不允许改变按钮的配色规则