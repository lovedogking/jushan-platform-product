# 真机联调准备任务书（Claude Code 用）

## 任务：臻识 C5H 真机联调准备与 V01-V04 验证

### 背景
一期 MVP 核心闭环（识别→计费→收费→放行）已在 Mock 环境下验证通过。当前最大阻塞是**无真机 MQTT 连接**，导致开闸/校时/显示屏/语音全部超时。需配置臻识 C5H 真机，完成 V01-V04 验证。

### 部署架构
```
服务器（1 台）
├── Platform Spring Boot  :8080
├── Device Access         :8081
├── MySQL                 :3307
├── Redis                 :6378
└── 臻识 C5H 相机        :80 (HTTP) / 1883 (MQTT)
    └── 道闸控制器        :接相机继电器输出
```

---

## 子任务 1：真机网络与接入配置（30 分钟）

### 目标
让 Device Access 服务能通过网络访问到臻识 C5H 相机。

### 执行步骤

1. **确认相机网络参数**
   - 相机 IP 地址（如 192.168.1.100）
   - 子网掩码、网关
   - 确认服务器与相机在同一网段或路由可达

2. **Device Access 配置更新**
   - 文件：`jushan-device-access/device-access-api/src/main/resources/application.yml`
   - 添加/更新：
     ```yaml
     mqtt:
       broker-url: tcp://${CAMERA_IP}:1883  # 臻识 MQTT 端口
       client-id: device-access-${random.uuid}
       username: ${MQTT_USER:admin}         # 臻识默认 admin
       password: ${MQTT_PASS:admin}         # 臻识默认 admin
     ```

3. **平台 Webhook 回调地址配置**
   - 文件：`jushan-platform/parking-boot/src/main/resources/application.yml`
   - 确认：
     ```yaml
     jushan:
       device-access:
         base-url: http://localhost:8081
         api-key: test-api-key-001
       webhook:
         allowed-ips: 127.0.0.1,localhost,192.168.1.100  # 添加相机 IP
     ```

4. **网络连通性测试**
   ```bash
   # 从服务器测试相机可达性
   ping 192.168.1.100
   curl http://192.168.1.100/api/status  # 臻识 HTTP 接口
   ```

### 验收标准
- [ ] 服务器能 ping 通相机 IP
- [ ] Device Access 启动无 MQTT 连接错误（或错误为"连接中"而非"拒绝连接"）
- [ ] 平台 Webhook 白名单包含相机 IP

---

## 子任务 2：设备台账同步（30 分钟）

### 目标
确保平台设备台账中的 SN 与 Device Access 侧一致，真机设备能在两端正确识别。

### 执行步骤

1. **获取臻识 C5H 设备 SN**
   - 登录相机 Web 管理界面（http://192.168.1.100）
   - 查看设备信息页，记录 SN（如 ZS-C5H-20240715-001）

2. **Device Access 注册设备**
   ```bash
   curl -X POST http://localhost:8081/api/v1/devices \
     -H "Content-Type: application/json" \
     -H "X-API-Key: test-api-key-001" \
     -d '{
       "deviceSn": "ZS-C5H-20240715-001",
       "vendor": "ZHENSHI",
       "model": "C5H",
       "type": "CAMERA",
       "name": "入口相机-臻识C5H"
     }'
   ```
   - 记录返回的 deviceId

3. **平台侧创建设备台账**
   - 登录 admin-web（http://localhost:3000）
   - 进入"设备管理" → "添加设备"
   - 填写：
     - 设备名称：入口相机-臻识C5H
     - 厂商：臻识
     - 型号：C5H
     - 设备 SN：ZS-C5H-20240715-001（**必须与 DA 侧一致**）
     - 绑定停车场：测试停车场
     - 绑定车道：入口车道
   - 保存并记录平台设备 ID

4. **验证 SN 映射一致性**
   - 平台设备台账中的 `device_sn` = DA 侧的 `deviceSn`
   - 不一致会导致开闸调用失败

### 验收标准
- [ ] Device Access 侧设备注册成功，返回 deviceId
- [ ] 平台侧设备台账创建成功，SN 与 DA 侧一致
- [ ] 平台侧设备绑定到正确停车场和车道

---

## 子任务 3：V01 验证 — C5H 型号和固件（20 分钟）

### 目标
确认 Device Access 能正确识别 C5H 型号，平台侧能查询到设备状态。

### 执行步骤

1. **Device Access 查询设备状态**
   ```bash
   curl http://localhost:8081/api/v1/devices/{deviceId}/status \
     -H "X-API-Key: test-api-key-001"
   ```
   - 预期返回：设备型号 C5H、固件版本、在线状态

2. **平台侧查询设备状态**
   - admin-web 设备管理页点击"刷新状态"
   - 或调用后端 API：
     ```bash
     curl http://localhost:8080/api/admin/devices/{id}/status \
       -H "Authorization: Bearer ${TOKEN}"
     ```

3. **验证状态字段**
   - 确认返回包含：`model: "C5H"`、`vendor: "ZHENSHI"`、`online: true/false`

### 验收标准
- [ ] Device Access 状态查询返回 C5H 型号信息
- [ ] 平台侧状态查询与 DA 侧一致
- [ ] 设备显示在线（MQTT 连接成功）

---

## 子任务 4：V02 验证 — 真实车牌识别事件（30 分钟）

### 目标
触发真实车牌识别，验证 Device Access → 平台 Webhook 推送链路。

### 执行步骤

1. **配置 Webhook 回调地址**
   - Device Access 侧配置平台 Webhook URL：
     ```
     http://192.168.1.10:8080/api/v1/device-webhook/events  # 服务器 IP
     ```
   - 确认 IP 白名单已配置（子任务 1）

2. **触发真实识别**
   - 在相机前放置真实车辆（或打印车牌照片）
   - 观察相机 LED 屏是否显示识别结果

3. **验证 Device Access 收到识别事件**
   - 查看 Device Access 日志：
     ```
     PlateRecognizedEvent: plateNumber=京A12345, confidence=0.98, ...
     ```

4. **验证平台收到 Webhook**
   - 查看平台日志：
     ```
     DeviceWebhookController: received event, eventId=..., plateNumber=京A12345
     ```
   - 检查数据库：
     ```sql
     SELECT * FROM parking_session WHERE plate_number = '京A12345' ORDER BY id DESC LIMIT 1;
     ```
   - 预期：有记录，状态=IN，entry_time 正确

5. **验证字段映射**
   - 对比相机原始字段与平台接收字段：
     | 相机字段 | 平台字段 | 说明 |
     |----------|---------|------|
     | plateNumber | plateNumber | 车牌号 |
     | plateColor | plateColor | 车牌颜色 |
     | captureTime | captureTime | 识别时间 |
     | deviceSn | deviceSn | 设备 SN |
     | confidence | confidence | 置信度 |

### 验收标准
- [ ] 真实车辆触发识别，相机 LED 显示车牌
- [ ] Device Access 日志显示 PlateRecognizedEvent
- [ ] 平台收到 Webhook，创建 ParkingSession
- [ ] 数据库记录与真实车牌一致
- [ ] 字段映射无丢失

---

## 子任务 5：V03 验证 — 真实心跳（15 分钟）

### 目标
验证设备心跳正常，平台能感知设备在线状态。

### 执行步骤

1. **观察心跳日志**
   - Device Access 日志：
     ```
     DeviceHeartbeatRecorder: deviceSn=ZS-C5H-..., interval=30s
     ```
   - 平台侧（如有心跳查询接口）：
     ```bash
     curl http://localhost:8080/api/admin/devices/{id}/status
     ```

2. **验证心跳频率**
   - 臻识 C5H 默认心跳间隔：30 秒
   - 确认日志中每 30 秒有一条心跳记录

3. **模拟断网测试**
   - 临时断开相机网络
   - 观察 Device Access 心跳超时日志
   - 恢复网络后观察心跳恢复

### 验收标准
- [ ] 心跳日志每 30 秒出现一次
- [ ] 断网后心跳超时，设备状态变为离线
- [ ] 恢复网络后心跳恢复，设备状态变为在线

---

## 子任务 6：V04 验证 — 真实开闸与闸杆动作（30 分钟）

### 目标
验证开闸命令从平台 → Device Access → 相机 → 道闸的完整链路，确认闸杆实际动作。

### 执行步骤

1. **准备测试环境**
   - 确认道闸电源开启
   - 确认道闸与相机继电器连接正确
   - 确认测试区域安全，无车辆/人员

2. **平台侧触发开闸（人工开闸）**
   - admin-web 设备管理页 → 找到 C5H 设备 → 点击"开闸"
   - 或调用 API：
     ```bash
     curl -X POST http://localhost:8080/api/admin/devices/{id}/open-gate \
       -H "Authorization: Bearer ${TOKEN}"
     ```

3. **观察调用链路**
   - 平台日志：
     ```
     DeviceService.openGate: deviceSn=ZS-C5H-..., calling Device Access
     ```
   - Device Access 日志：
     ```
     GateController: received open gate request, deviceSn=...
     MQTT publish: topic=.../gate/open, payload=...
     ```
   - 相机日志（如有）：
     ```
     Received gate open command, executing...
     ```

4. **验证闸杆动作**
   - **肉眼观察**：闸杆是否抬起
   - **声音确认**：道闸电机是否有动作声
   - **相机 LED**：是否显示"开闸成功"或类似提示

5. **验证三层状态**
   - 平台侧查询开闸结果：
     ```sql
     SELECT * FROM device_command_audit WHERE device_sn='ZS-C5H-...' ORDER BY id DESC LIMIT 1;
     ```
   - 预期：
     - `command_sent=true`（命令已发送）
     - `device_ack=true`（设备回复成功）
     - `gate_opened=null`（一期无法确认，需人工观察）

6. **验证关闸**
   - admin-web 点击"关闸"
   - 观察闸杆是否落下

### 验收标准
- [ ] 平台开闸 API 返回成功
- [ ] Device Access MQTT 下发成功
- [ ] 闸杆实际抬起（肉眼确认）
- [ ] 数据库记录 command_sent=true, device_ack=true
- [ ] 关闸后闸杆实际落下
- [ ] 无自动重试（AGENTS.md P0 约束）

---

## 子任务 7：显示屏与语音验证（20 分钟）

### 目标
验证显示屏文字和语音播报功能。

### 执行步骤

1. **显示屏文字**
   - admin-web → 设备控制 → "显示屏" Tab
   - 输入文字："欢迎光临"，点击发送
   - 观察相机 LED 屏是否显示

2. **语音播报**
   - admin-web → 设备控制 → "语音" Tab
   - 选择模板（如"请缴费{amount}元"），输入金额 5.00
   - 点击播放
   - 观察相机喇叭是否播报

### 验收标准
- [ ] 显示屏文字正确显示
- [ ] 语音播报内容正确
- [ ] 参数替换正确（如 {amount} → 5.00）

---

## 全局约束

1. **安全**：测试区域无车辆/人员，防止闸杆误伤
2. **无自动重试**：开闸失败不得自动重试，标记 UNCERTAIN 后人工处理
3. **审计**：所有开闸操作记录到 device_command_audit
4. **幂等**：同一 eventId 不重复创建记录
5. **设备身份**：平台只信任自身台账中的 SN，不信任前端输入

---

## 验收标准

- [ ] V01 通过：C5H 型号和固件验证
- [ ] V02 通过：真实车牌识别事件推送
- [ ] V03 通过：心跳正常，在线/离线状态正确
- [ ] V04 通过：开闸/关闸闸杆实际动作
- [ ] 显示屏和语音功能正常
- [ ] 无 P0 安全违规
- [ ] 所有操作有审计记录

---

## 停止条件

- 发现 P0 安全违规 → 暂停并报告
- 设备硬件故障 → 联系厂商技术支持
- 网络不可达 → 排查网络配置

---

## 关联文档

- [一期联调准备清单](./docs/开发计划/一期联调准备清单.md)
- [AGENTS.md](./AGENTS.md) §4 Device Access 冻结契约
- [联调前阻塞项检查清单](./docs/开发计划/联调前阻塞项检查清单.md)
