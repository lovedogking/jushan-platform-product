# Claude Code 项目级执行手册

本文件补充 AGENTS.md。发生冲突时，以用户当前明确指令和 AGENTS.md 为准。

## 1. 指令优先级

Claude Code 必须按以下顺序理解任务：

    用户当前明确指令
        >
    当前任务书
        >
    AGENTS.md
        >
    CLAUDE.md
        >
    需求与接口文档
        >
    既有代码惯例

README.md、CODEX.md、docs/ 中的历史报告和旧提案只能作为辅助背景，不改变以上优先级。

当需求规格与 Device Access 接口规范冲突时，Device Access 接口规范的“当前已实现范围”和真机行为优先于目标架构。若任何上层指令与多租户、通行安全、支付安全、Device Access 边界或用户改动保护红线冲突，必须暂停相关实现，明确报告冲突、影响和可选方案，不得静默绕过红线。

## 2. Claude Code 的定位

Claude Code 是本项目的主要代码实施者，负责：

- 按用户或 Codex 已确认的任务书修改代码；
- 创建任务范围内的模块；
- 实现接口和数据库迁移；
- 编写前端页面和测试；
- 运行构建、测试及必要的人工验证；
- 修复实施过程中发现的局部问题；
- 输出完整实施报告。

Claude Code 不得擅自：

- 改变总体架构或扩大需求；
- 修改已冻结接口语义；
- 假设 Device Access 未实现能力；
- 重写与当前任务无关的模块；
- 删除或覆盖用户代码；
- 修改 Git 历史；
- commit 或 push。

发现方案级问题时，必须停止扩大修改并交给 Codex 或用户决策。Codex 的任务书是实施输入，不代表 Claude Code 可以跳过真实代码和测试检查。

## 3. 开始任务前的必读与检查

### 3.1 必读内容

每次任务必须按相关性阅读：

1. AGENTS.md；
2. 用户当前指令和当前任务书；
3. docs/需求文档/平台侧需求规格说明书.md；
4. docs/需求文档/停车业务平台与DeviceAccess接口规范.md；
5. README.md、CODEX.md 和 docs/ 中与任务有关的文件（存在才读取）；
6. 相关 Controller、Application Service、Domain/Service、Client、Mapper、Entity、DTO、VO；
7. 相关测试、数据库迁移、配置和前端调用代码。

文件名有版本号、括号或后缀时，应先搜索真实对应文件。若规范性文档缺失，涉及相应契约的实现必须标记为被阻塞并上报；不得用历史代码或旧提案补全缺失契约。

### 3.2 Git 与目录检查

修改前必须运行：

    git status
    git branch --show-current

随后使用当前环境可用的只读命令检查：

- 真实目录和构建清单；
- 任务涉及的源文件和测试；
- 相似实现与公共约定；
- 数据库迁移和表约束；
- 前后端真实调用链；
- 当前未提交改动及其归属。

优先使用 rg 和 rg --files 搜索。禁止只看文件名猜测实现，禁止以旧验证报告替代当前检查。

## 4. 理解任务书

开始实施前，必须从任务书提取并确认：

- 目标；
- 允许修改范围；
- 禁止修改范围；
- 输入文档和已冻结契约；
- 验收标准；
- 测试命令或验证方法；
- 回滚方式；
- 待确认事项和外部依赖。

缺失信息不影响安全且可从真实代码确认时，可以做最小、可逆的合理假设，并在报告中说明。缺失信息会改变业务语义、数据结构、外部接口、资金或通行安全时，必须暂停对应部分并请求用户或 Codex 决策。

Claude Code 禁止把一个任务书扩展为整个平台开发，也禁止为了统一风格顺手修改无关代码。

## 5. 复杂任务开始格式

复杂任务开始时先输出：

    目标
    允许修改范围
    禁止修改范围
    计划步骤
    验证方式
    已识别风险

简单且低风险的局部修复可以缩短说明，但必须明确修改边界和验证方式。

## 6. 小步实施流程

按以下顺序执行：

1. 建立当前行为、失败现象或契约基线；
2. 找到真实调用链和最小修改点；
3. 先补或调整能证明行为的测试；
4. 完成一个最小代码或迁移改动；
5. 立即运行相关测试；
6. 检查权限、租户、停车场、并发、幂等和异常路径；
7. 再进入下一小步；
8. 最后运行与改动范围相称的完整验证。

每一步必须可解释、可验证、可回退。禁止一次创建大量空模块，禁止为“未来可能需要”引入复杂抽象或基础设施。

数据库任务必须使用新的 Flyway 版本迁移，禁止修改已发布迁移。金额或状态更新必须是精确字段、带条件的更新；禁止用携带未知字段的整实体 updateById 覆盖订单、支付或停车记录。

## 7. 不明确需求和外部资料

以下内容未确认时禁止编造：

- Device Access 新接口、事件、错误码和设备能力；
- 厂商协议字段和 MQTT Topic；
- 微信支付参数、商户号、AppId、回调地址和密钥；
- 服务器地址；
- 停车场真实收费规则；
- 真机能力、固件行为和第三方资料。

处理步骤：

1. 使用不降低安全性的接口抽象或配置占位；
2. 将缺口写入待确认事项；
3. 标明被阻塞的功能、原因和风险；
4. 继续完成不依赖该信息的部分；
5. 禁止将占位、Mock 或目标设计标成已完成联调。

若实现需要新增外部依赖、改变主框架、拆分微服务、引入 Kubernetes 或更换数据库，必须停止扩大修改并上报。

## 8. 多租户与权限执行检查

所有读写、统计、导出、批处理、定时任务、消息消费和 WebSocket 推送都必须检查：

1. 当前操作人身份；
2. 可信租户上下文；
3. 授权停车场范围；
4. 车道和设备绑定；
5. 查询与更新条件是否包含正确数据范围；
6. 非法范围是否失败关闭；
7. 超级管理员代操作是否记录真实操作人和目标范围。

禁止直接信任前端传入的 tenantId、parkingLotId 或设备 SN。前端隐藏按钮和菜单不能代替后端鉴权。

任何任务涉及租户或停车场数据时，至少增加：

- 本租户成功用例；
- 跨租户拒绝用例；
- 跨停车场拒绝用例；
- 无权限拒绝用例；
- 非法或缺失范围失败关闭用例。

任一越权或隔离缺失均为 P0，必须停止交付。

## 9. Device Access 执行手册

### 9.1 固定边界

本项目只实现停车业务平台侧。Device Access 由其他开发人员维护，本仓库禁止修改 Device Access。

平台禁止：

- 直接连接厂商设备；
- 解析臻识、信路通等厂商协议；
- 拼装厂商 MQTT Topic；
- 调用厂商 SDK、DLL 或 SO；
- 让浏览器或小程序直接访问 Device Access；
- 使用未冻结的目标接口。

平台设备模块只能实现平台设备台账、可信 SN 映射、统一 DeviceAccessClient、调用审计、状态展示和平台业务编排。

### 9.2 当前唯一可调用接口

当前只允许：

    POST /api/v1/devices/{deviceSn}/gate/open
    POST /api/v1/devices/{deviceSn}/time/sync
    GET  /api/v1/devices/{deviceSn}/status

接口规范可能将路径变量写成 {deviceId}，但实际值必须是 device_sn。实现必须：

1. 接收平台设备主键或业务引用；
2. 在后端查询设备记录；
3. 校验权限、租户、停车场、车道、启用状态和设备能力；
4. 从可信记录读取 device_sn；
5. 通过统一客户端调用；
6. 保存结果和审计。

禁止把前端输入的 SN 直接透传。

### 9.3 开闸安全

必须按以下当前事实实现：

- 开闸和校时同步等待设备回复；
- Device Access 内部最长等待约 10 秒；
- 当前没有 commandId 幂等；
- 每次开闸调用都可能触发真实动作；
- HTTP 或网络超时不代表道闸未动作；
- 开闸请求禁止 HTTP 客户端、网关、注解、消息队列或业务层自动重试；
- 网络错误、命令超时和平台客户端读取超时标记为 UNCERTAIN；
- UNCERTAIN 只能通过状态查询、现场确认或岗亭人工处置；
- 人工再次开闸必须记录原因、操作人、前次结果和现场确认信息。

开闸前必须创建或准备持久化调用审计。测试自动重试策略时必须使用 HTTP Mock，禁止用真机制造重复动作。

### 9.4 当前未确认能力

以下能力一律不得实现成真实调用：

- 车牌识别事件上报；
- RabbitMQ 设备事件、Exchange、Queue 和 Routing Key；
- HMAC 服务认证；
- 图片上传和补传；
- commandId 幂等和命令查询；
- 关闸、常开、取消常开、抓拍、重启；
- 其他未进入接口规范“当前已实现范围”的能力。

可以设计不产生错误承诺的接口抽象，但必须标记待确认，不得假设 Device Access 服务端支持。Device Access 当前应部署在可信内网并限制调用源。

## 10. 支付与资金执行检查

支付任务实施前和完成后必须检查：

- 金额使用整数分或已确认的精确 Decimal，禁止浮点数；
- 负金额被拒绝；
- 退款金额不超过实际支付金额；
- 支付回调验签；
- appid、mchid、outTradeNo 和金额均校验；
- 重复回调幂等；
- 小程序返回不作为最终支付结果；
- 服务端查询或验签回调决定最终状态；
- 支付成功与开闸解耦，并有补偿与人工兜底；
- 零元订单有明确状态流转；
- 日志不记录密钥、完整签名、证书私钥和敏感凭证。
- 日志不输出密码、Token、身份证号、手机号等敏感信息明文。

任何支付链路修改都必须有正常、重复、伪造、金额不一致、非法状态和异常补偿测试。缺少支付资料时不得伪造生产参数，应完成不依赖资料的代码并列出联调阻塞项。

## 11. 构建、测试与失败处理

### 11.1 选择真实命令

先从 pom.xml、Maven Wrapper、package.json、锁文件、Docker Compose 文件和现有脚本确认命令。只运行项目真实存在且与本次修改有关的命令。

任何修改完成后至少运行 git diff --check；任务结束时重新运行 git status，确认没有混入无关文件。

按范围至少执行：

- 编译；
- 单元测试；
- 相关模块测试；
- 数据库迁移检查；
- 接口测试；
- 权限和数据隔离测试；
- 幂等测试；
- 异常路径测试。

基础设施集成优先使用 Testcontainers。Device Access 集成必须使用 WireMock、MockWebServer 或项目已确认的等价 HTTP Mock 工具。

### 11.2 Device Access 最低测试矩阵

必须覆盖：

    HTTP 200 成功
    404 设备不存在
    503 MQTT 不可用
    500 设备错误
    约 10 秒命令超时
    平台客户端超时
    响应解析失败
    跨停车场调用
    重复开闸调用
    UNCERTAIN 状态
    审计记录

重复开闸测试应证明没有自动重试。禁止只验证正常路径。

### 11.3 测试失败

测试失败时：

1. 保留完整失败命令；
2. 提取首个根因和必要日志；
3. 判断是本轮回归、既有失败还是环境阻塞；
4. 只修复任务范围内的根因；
5. 修复后重新运行相关测试；
6. 无法修复时停止扩大修改并报告风险。

不得注释核心逻辑、删除测试、放宽鉴权或伪造结果来获得绿色构建。

无法运行某项测试时，报告必须写明未运行项、原因、风险和后续命令或人工步骤。禁止使用“理论上可以”代替验证。

## 12. 用户未提交改动与 Git

默认规则：

- 不得 commit；
- 不得 push；
- 不得 force push；
- 不得 rebase 用户分支；
- 不得 reset --hard；
- 不得清理、覆盖或格式化用户未提交改动；
- 不得无条件覆盖用户已有文件或其中仍有效的内容；
- 不得把无关文件加入结果；
- 不得修改 Git 历史。

用户明确要求 commit 或 push 前，仍必须重新确认范围和 Git 状态。若用户已有改动与任务文件重叠，先阅读 diff 并尽量做局部合并；无法保证不覆盖时，立即停止并上报。

## 13. 必须停止扩大修改并上报的情况

出现以下任一情况时必须停止相关实现：

- 需要改变总体架构或模块边界；
- 需要扩大用户确认的需求范围；
- 需求与当前接口规范冲突；
- 所需 Device Access 能力不在当前已实现范围；
- 车牌识别事件、RabbitMQ、HMAC、图片或新控制接口尚未冻结；
- 涉及支付、真实收费规则或第三方参数但资料缺失；
- 发现跨租户、跨停车场或越权风险；
- 需要破坏性数据库或 Git 操作；
- 现有未提交改动无法安全绕开；
- 测试持续失败且根因超出任务范围；
- 上层指令与安全红线冲突。

上报内容应包含证据、受影响功能、已经完成的安全部分、阻塞原因、可选方案和需要谁决策。

## 14. 完成报告

任务完成时必须输出：

    1. 完成内容
    2. 修改文件
    3. 数据库变更
    4. 接口变更
    5. 测试和验证结果
    6. 未完成内容
    7. 已知风险
    8. 待确认事项
    9. 建议下一步
    10. Git 状态

没有数据库或接口变更时明确写“无”。Git 状态必须基于任务结束时的实际命令，禁止只写“已完成”。

Bug 修复必须在“完成内容”或“测试和验证结果”中说明根因和验证方式。

## 15. 协作规范维护

除非当前任务明确允许，Claude Code 不得顺手修改 AGENTS.md 或 CLAUDE.md。技术栈、模块边界、Device Access 正式接口、车牌事件契约、支付模式、Git 工作流、AI 职责、测试或部署方式发生变化时，必须建议同步审查两份文件。

修改协作规范本身时，实施报告必须说明修改原因、影响范围、是否改变工作流、是否需要更新其他文档，并再次检查两份文件的术语、接口事实、Git 规则和完成报告格式是否一致。
